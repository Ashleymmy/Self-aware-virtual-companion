# ============================================
# SAVC 环境配置模板
# 复制此文件为 config/.env.local 并填入实际值
# ============================================

# === LLM API 配置（可选：Phase 0 不强依赖）===
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# LLM 聊天主链路（多 Provider 降级）
ANYROUTER_API_KEY=
WZW_API_KEY=
# 语义检索 embedding 模型（Phase 4a）
EMBEDDING_MODEL=text-embedding-3-small
# 语义检索运行模式：prod(真实 API) / mock(测试) / local(本地 embedding 服务，如 Ollama)
SAVC_EMBEDDING_MODE=prod
# 语义检索参数（Phase 4a）
MEMORY_SEMANTIC_MIN_SCORE=0.3
MEMORY_SEMANTIC_DUPLICATE_THRESHOLD=0.95
MEMORY_SEMANTIC_HYBRID_ALPHA=0.7
# 时间衰减（Phase 4 收尾）
MEMORY_SEMANTIC_DECAY_ENABLED=true
MEMORY_SEMANTIC_DECAY_HALF_LIFE_DAYS=30
MEMORY_SEMANTIC_DECAY_MIN_FACTOR=0.35
# 本地 embedding（SAVC_EMBEDDING_MODE=local 时生效）
LOCAL_EMBEDDING_MODEL=nomic-embed-text
LOCAL_EMBEDDING_BASE_URL=http://127.0.0.1:11434
LOCAL_EMBEDDING_PATH=/api/embeddings
LOCAL_EMBEDDING_VECTOR_DIM=768
# 可选：自定义 OpenAI 兼容接口地址（例如代理 / 自建 / Azure）。通常需要包含 `/v1`
OPENAI_BASE_URL=
# 长期记忆嵌入（硅基流动 OpenAI 兼容接口）
SILICON_EMBEDDING_API_KEY=

# === 渠道配置（Phase 2 使用，Phase 0 仅预留）===
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=

# === Proactive 外部 API（Phase 2）===
GOOGLE_CALENDAR_ID=
GOOGLE_CALENDAR_SERVICE_ACCOUNT_JSON=
OPENWEATHER_API_KEY=
OPENWEATHER_LAT=
OPENWEATHER_LON=

# === 联网搜索 API（web_search）===
BRAVE_API_KEY=

# === OpenClaw 配置 ===
OPENCLAW_PORT=18789

# OpenClaw Workspace（指向 savc-core；默认相对 config/ 目录）
OPENCLAW_WORKSPACE=../savc-core

# OpenClaw Gateway 本机鉴权 Token（本地生成即可；仅用于本机 CLI/Loopback）
# 为空会导致 OpenClaw 配置加载失败（gateway.auth.token 需要环境变量替换）。
OPENCLAW_GATEWAY_TOKEN=

# === Phase 5d: 语音交互（默认 mock 验收）===
# 可选值: mock / twilio / telnyx / plivo
VOICE_CALL_PROVIDER=mock
# 非 mock provider 时可按需填写
VOICE_CALL_FROM_NUMBER=
VOICE_CALL_TO_NUMBER=
# provider 对应密钥（按需填写）
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TELNYX_API_KEY=
TELNYX_CONNECTION_ID=
TELNYX_PUBLIC_KEY=
PLIVO_AUTH_ID=
PLIVO_AUTH_TOKEN=
# 可选公网暴露（本地联调）
NGROK_AUTHTOKEN=
NGROK_DOMAIN=
# 语音可选增强
ELEVENLABS_API_KEY=
OPENAI_TTS_BASE_URL=
# 真人 TTS 参数预留（后续可用于自动注入 voice-call.config.tts）
# provider: openai | elevenlabs
VOICE_TTS_PROVIDER=openai
VOICE_TTS_OPENAI_MODEL=gpt-4o-mini-tts
VOICE_TTS_OPENAI_VOICE=alloy
VOICE_TTS_OPENAI_INSTRUCTIONS=
VOICE_TTS_ELEVENLABS_MODEL_ID=eleven_turbo_v2_5
VOICE_TTS_ELEVENLABS_VOICE_ID=
VOICE_TTS_ELEVENLABS_BASE_URL=

# === Phase 5e: 视觉能力 / 图像生成 ===
# real 模式图像生成需要 OPENAI_API_KEY
OPENAI_IMAGE_MODEL=gpt-image-1
# 是否启用 Phase 5e live smoke（0=关闭，1=开启）
PHASE5E_IMAGE_LIVE=0

# === Phase 6: Live2D 前端桥接（savc-ui）===
# 模式: auto（优先网关失败回退 mock）| gateway（仅网关）| mock（仅本地）
VITE_SAVC_UI_LIVE2D_MODE=auto
VITE_SAVC_LIVE2D_MODEL_URL=/live2d/yuanyuan-lite.model.json
VITE_SAVC_GATEWAY_URL=http://127.0.0.1:18789
VITE_SAVC_GATEWAY_TOKEN=
VITE_SAVC_SESSION_KEY=main
