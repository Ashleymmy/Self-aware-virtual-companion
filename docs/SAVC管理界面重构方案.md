# SAVC 管理界面重构方案

> 版本: 1.0
> 日期: 2026-02-12
> 状态: **方案设计**
> 基础: 基于 OpenClaw Control UI (Lit 3 + Vite 7) 重构

---

## 1. 背景与目标

### 1.1 现状分析

当前 SAVC 使用 OpenClaw 原生 Control UI 作为管理界面，存在以下问题：

| 问题 | 详情 |
|------|------|
| 语言障碍 | 所有 UI 文本为英文，标签/提示/占位符均无中文支持 |
| 交互感弱 | 纯功能导向，缺少过渡动画、微交互反馈和视觉层次 |
| 定制缺失 | SAVC 特有功能（记忆管理、人格配置、多 Agent 编排）无专属视图 |
| 信息密度高 | 面板信息过于平铺，缺少折叠/分组/渐进式展示 |
| 移动端体验差 | 虽有 `layout.mobile.css` 但交互体验未针对触屏优化 |

### 1.2 设计目标

1. **全中文界面** — 所有可见文本中文化，包括导航、表单标签、状态提示、错误信息
2. **强交互感** — 丰富的微交互动画、状态过渡、拖拽操作、实时数据可视化
3. **SAVC 定制** — 为媛媛的记忆系统、人格管理、Agent 编排提供专属管理面板
4. **渐进式信息架构** — 概览 → 详情 → 操作，按需展开，降低认知负荷
5. **响应式体验** — 桌面/平板/移动端均有良好的交互体验

### 1.3 可行性评估

| 维度 | 评估 | 说明 |
|------|------|------|
| 技术栈复用 | ✅ 完全可行 | 继续使用 Lit 3 + Vite 7，复用现有 CSS 变量体系和组件 |
| 样式复用 | ✅ 高度复用 | `base.css` 的变量体系、动画系统、主题切换机制直接继承 |
| 组件复用 | ✅ 大量复用 | Card、Button、Form Field、Pill、StatusDot 等组件直接使用 |
| 数据层复用 | ✅ 完全复用 | Gateway WebSocket 通信、Controllers 层逻辑无需改动 |
| 构建配置 | ✅ 零改动 | Vite 构建配置、打包输出路径不变 |
| 新增工作量 | 中等 | 主要在视图层重构 + 中文化 + 新增 SAVC 专属面板 |

**结论：完全可行。** 基于 OpenClaw 现有架构做增量重构，不需要推翻重来。

---

## 2. 架构设计

### 2.1 整体架构

采用 **「壳层不动、视图重写、能力新增」** 的策略：

```
重构范围划分
─────────────────────────────────────────
不动层（直接复用）:
├── Gateway WebSocket 通信层
├── Controllers 数据控制层
├── CSS 变量体系 (base.css 中的 :root 变量)
├── 主题系统 (theme.ts + light/dark 切换)
└── 构建配置 (vite.config.ts)

改造层（修改适配）:
├── app-render.ts     → 重写导航结构与视图路由
├── navigation.ts     → 重新定义 Tab 分组与中文标签
├── layout.css        → 优化布局结构（保留 grid 方案）
├── components.css    → 增强组件动画与交互效果
└── index.html        → 调整 lang="zh-CN"，引入中文字体

新增层（SAVC 专属）:
├── views/dashboard.ts     → 全新中文仪表盘
├── views/memory.ts        → 记忆系统管理面板
├── views/persona.ts       → 人格配置管理面板
├── views/orchestrator.ts  → Agent 编排可视化
├── i18n/zh-CN.ts          → 中文文本字典
└── styles/savc.css        → SAVC 专属样式增强
```

### 2.2 目录结构变化

```
openclaw/ui/src/
├── ui/
│   ├── i18n/                    ← 新增
│   │   ├── index.ts             # i18n 入口，提供 t() 函数
│   │   └── zh-CN.ts             # 中文文本字典
│   ├── views/
│   │   ├── dashboard.ts         ← 新增：中文仪表盘（替代 overview.ts）
│   │   ├── memory.ts            ← 新增：记忆系统管理
│   │   ├── persona.ts           ← 新增：人格配置
│   │   ├── orchestrator.ts      ← 新增：Agent 编排
│   │   ├── agents.ts            # 保留，文本中文化
│   │   ├── channels.ts          # 保留，文本中文化
│   │   ├── chat.ts              # 保留，文本中文化
│   │   ├── config.ts            # 保留，文本中文化
│   │   ├── sessions.ts          # 保留，文本中文化
│   │   ├── logs.ts              # 保留，文本中文化
│   │   └── ...
│   ├── styles/
│   │   └── savc.css             ← 新增：SAVC 扩展样式
│   └── ...
└── styles/
    └── base.css                 # 字体栈增加中文字体
```

### 2.3 中文化策略

采用 **轻量级 i18n 方案**（不引入第三方库），符合 OpenClaw 的简洁哲学：

```typescript
// i18n/zh-CN.ts
export const zhCN = {
  // 导航
  nav: {
    chat: '对话',
    dashboard: '仪表盘',
    channels: '渠道管理',
    instances: '实例',
    sessions: '会话',
    cron: '定时任务',
    agents: 'Agent 管理',
    skills: '技能',
    memory: '记忆系统',
    persona: '人格配置',
    orchestrator: '编排中心',
    config: '系统设置',
    debug: '调试',
    logs: '日志',
  },
  // 导航分组
  navGroup: {
    chat: '对话',
    control: '控制台',
    savc: '媛媛',
    agent: 'Agent',
    settings: '设置',
  },
  // 仪表盘
  dashboard: {
    title: '仪表盘',
    subtitle: '系统状态总览',
    gatewayAccess: '网关连接',
    snapshot: '运行快照',
    status: '状态',
    uptime: '运行时间',
    connected: '已连接',
    disconnected: '已断开',
    instances: '实例数',
    sessions: '会话数',
    cronStatus: '定时任务',
    enabled: '已启用',
    disabled: '已停用',
    connect: '连接',
    refresh: '刷新',
  },
  // 记忆系统
  memory: {
    title: '记忆系统',
    subtitle: '管理媛媛的长期记忆与语义检索',
    totalMemories: '记忆总数',
    recentCaptures: '最近捕获',
    searchMemory: '搜索记忆...',
    semantic: '语义搜索',
    keyword: '关键词搜索',
    hybrid: '混合搜索',
    decay: '时间衰减',
    importance: '重要程度',
    lastAccess: '最近访问',
    category: '分类',
    source: '来源',
    noMemories: '暂无记忆记录',
  },
  // 人格配置
  persona: {
    title: '人格配置',
    subtitle: '定义媛媛的性格、语气和价值观',
    soul: '灵魂设定',
    voice: '语气风格',
    values: '价值观',
    identity: '身份认同',
    preview: '对话预览',
  },
  // 编排中心
  orchestrator: {
    title: '编排中心',
    subtitle: '多 Agent 协同编排与调度',
    activeAgents: '活跃 Agent',
    routingRules: '路由规则',
    recentDispatches: '最近调度',
    flowEditor: '流程编辑',
  },
  // 通用
  common: {
    save: '保存',
    cancel: '取消',
    delete: '删除',
    edit: '编辑',
    add: '添加',
    search: '搜索',
    loading: '加载中...',
    noData: '暂无数据',
    confirm: '确认',
    reset: '重置',
    export: '导出',
    import: '导入',
    close: '关闭',
    back: '返回',
    next: '下一步',
    previous: '上一步',
    enabled: '已启用',
    disabled: '已停用',
    success: '操作成功',
    error: '操作失败',
  },
} as const;

// i18n/index.ts
import { zhCN } from './zh-CN.ts';
type DeepKeys<T> = /* 嵌套键路径类型 */;
export function t(key: DeepKeys<typeof zhCN>): string { /* 按路径取值 */ }
```

---

## 3. 导航重构

### 3.1 新导航结构

从 4 组 12 个 Tab 扩展为 **5 组 15 个 Tab**：

```
新 TAB_GROUPS:
──────────────────────────────────

[对话]
  └── 对话                    # chat      (保留)

[控制台]
  ├── 仪表盘                  # dashboard (新增，替代 overview)
  ├── 渠道管理                # channels  (保留)
  ├── 实例                    # instances (保留)
  ├── 会话                    # sessions  (保留)
  └── 定时任务                # cron      (保留)

[媛媛]     ← 新增分组
  ├── 记忆系统                # memory       (新增)
  ├── 人格配置                # persona      (新增)
  └── 编排中心                # orchestrator (新增)

[Agent]
  ├── Agent 管理              # agents  (保留)
  ├── 技能                    # skills  (保留)
  └── 节点                    # nodes   (保留)

[设置]
  ├── 系统设置                # config  (保留)
  ├── 调试                    # debug   (保留)
  └── 日志                    # logs    (保留)
```

### 3.2 导航交互增强

| 增强项 | 实现方式 |
|--------|----------|
| 侧边栏折叠动画 | CSS `transition` + `will-change: width` 平滑过渡 |
| 当前页高亮 | 保留现有 `.nav-item.active` 样式，增加左侧指示条 `::before` |
| 悬浮预览 | 折叠态下鼠标悬停显示 tooltip（纯 CSS `::after`） |
| 键盘导航 | 添加 `tabindex` 和 `@keydown` 快捷键支持 |
| 分组折叠记忆 | 折叠状态存入 `localStorage` |

---

## 4. 视图设计

### 4.1 仪表盘 (Dashboard)

替代原 `overview.ts`，提供更丰富的系统概览：

```
┌─────────────────────────────────────────────────┐
│  仪表盘                                        │
│  系统状态总览                                   │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 状态     │  │ 运行时间 │  │ 活跃会话 │      │
│  │ ● 已连接 │  │ 3天 12h  │  │ 5        │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│                                                 │
│  ┌─ 网关连接 ────────────────────────────────┐  │
│  │  WebSocket URL    [ws://127.0.0.1:18789]  │  │
│  │  Token            [••••••••••••]          │  │
│  │                                           │  │
│  │  [连接]  [刷新]                           │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
│  ┌─ 媛媛状态 ───────┐  ┌─ 最近活动 ─────────┐  │
│  │  当前模式: 陪伴   │  │  14:32 用户对话     │  │
│  │  活跃Agent: 3     │  │  14:28 记忆捕获     │  │
│  │  记忆条目: 128    │  │  14:15 定时触发     │  │
│  │  今日对话: 12     │  │  13:50 技能调用     │  │
│  └──────────────────┘  └─────────────────────┘  │
│                                                 │
│  ┌─ 快捷操作 ────────────────────────────────┐  │
│  │  [管理记忆]  [编辑人格]  [查看日志]       │  │
│  └───────────────────────────────────────────┘  │
│                                                 │
└─────────────────────────────────────────────────┘
```

**交互增强：**
- 状态卡片带数字跳动动画（`@keyframes countUp`）
- 连接状态实时脉动指示（复用 `.statusDot` 动画）
- 最近活动列表实时推送（WebSocket 事件驱动）
- 快捷操作按钮悬停微动效（`transform: translateY(-2px)`）

### 4.2 记忆系统管理 (Memory)

SAVC 专属面板，管理媛媛的记忆数据：

```
┌─────────────────────────────────────────────────┐
│  记忆系统                                       │
│  管理媛媛的长期记忆与语义检索                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 记忆总数 │  │ 最近捕获 │  │ 衰减预警 │      │
│  │ 128      │  │ 今日 5条  │  │ 3条      │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│                                                 │
│  ┌─ 搜索 ───────────────────────────────────┐   │
│  │ 🔍 [搜索记忆...                        ] │   │
│  │ [语义] [关键词] [混合]     [导出] [导入] │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─ 记忆列表 ───────────────────────────────┐   │
│  │ ┌────────────────────────────────────────┐│   │
│  │ │ 用户喜欢深夜聊天                      ││   │
│  │ │ 分类: 习惯偏好  重要度: ●●●○○         ││   │
│  │ │ 来源: 对话推断  2026-02-10 23:41      ││   │
│  │ │                          [编辑] [删除]││   │
│  │ └────────────────────────────────────────┘│   │
│  │ ┌────────────────────────────────────────┐│   │
│  │ │ 用户是程序员，擅长 TypeScript          ││   │
│  │ │ 分类: 基本信息  重要度: ●●●●●          ││   │
│  │ │ 来源: 自我介绍  2026-02-08 14:22      ││   │
│  │ │                          [编辑] [删除]││   │
│  │ └────────────────────────────────────────┘│   │
│  │                                           │   │
│  │          [加载更多...]                    │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

**数据接口：** 通过 Gateway WebSocket 调用 `memory.*` 系列方法，读取 `/savc-core/memory/` 下的 Markdown 文件和 LanceDB 向量数据。

**交互增强：**
- 搜索模式切换带滑动动画（复用 `.cfg-segmented` 组件）
- 记忆卡片展开/折叠带弹性过渡（`ease-spring`）
- 重要度以圆点可视化，支持点击调整
- 衰减预警卡片闪烁提示（复用 `pulse-subtle` 动画）
- 搜索结果高亮匹配文本

### 4.3 人格配置 (Persona)

管理 `SOUL.md`、`voice.yaml`、`values.yaml` 等人格相关文件：

```
┌─────────────────────────────────────────────────┐
│  人格配置                                       │
│  定义媛媛的性格、语气和价值观                    │
├─────────────────────────────────────────────────┤
│                                                 │
│  [灵魂设定] [语气风格] [价值观] [对话预览]      │
│                                                 │
│  ┌─ 灵魂设定 (SOUL.md) ─────────────────────┐   │
│  │                                           │   │
│  │  ┌─ 基本身份 ─────────────────────────┐   │   │
│  │  │ 名称: 媛媛                         │   │   │
│  │  │ 核心人设: 温暖、体贴、有趣         │   │   │
│  │  │ 关系定位: 亲密伙伴                 │   │   │
│  │  └────────────────────────────────────┘   │   │
│  │                                           │   │
│  │  ┌─ 性格特征 ─────────────────────────┐   │   │
│  │  │ ● 共情力 ████████░░ 80%            │   │   │
│  │  │ ● 幽默感 ██████░░░░ 60%            │   │   │
│  │  │ ● 好奇心 █████████░ 90%            │   │   │
│  │  │ ● 独立性 ███████░░░ 70%            │   │   │
│  │  └────────────────────────────────────┘   │   │
│  │                                           │   │
│  │  ┌─ 原始文件编辑 ─────────────────────┐   │   │
│  │  │ [Markdown 编辑器]                  │   │   │
│  │  └────────────────────────────────────┘   │   │
│  │                                           │   │
│  │  [保存修改]  [恢复默认]                   │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

**交互增强：**
- 性格特征进度条支持拖拽调整
- Tab 页切换带水平滑动过渡
- 原始文件编辑器实时预览
- 对话预览 Tab 可模拟不同性格参数下的回复风格

### 4.4 编排中心 (Orchestrator)

可视化展示多 Agent 协同关系：

```
┌─────────────────────────────────────────────────┐
│  编排中心                                       │
│  多 Agent 协同编排与调度                        │
├─────────────────────────────────────────────────┤
│                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐      │
│  │ 活跃Agt  │  │ 今日调度 │  │ 平均延迟 │      │
│  │ 4 / 7    │  │ 23 次    │  │ 1.2s     │      │
│  └──────────┘  └──────────┘  └──────────┘      │
│                                                 │
│  ┌─ Agent 拓扑图 ───────────────────────────┐   │
│  │                                           │   │
│  │     ┌────────────┐                        │   │
│  │     │ Orchestr.  │ ← 入口                │   │
│  │     └─────┬──────┘                        │   │
│  │       ┌───┼───┬───────┐                   │   │
│  │       ▼   ▼   ▼       ▼                   │   │
│  │   [Companion] [Tech] [Creative] [Memory]  │   │
│  │    陪伴Agent   技术    创意      记忆     │   │
│  │                                           │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─ 路由规则 ───────────────────────────────┐   │
│  │  日常对话 → companion (Sonnet 4)          │   │
│  │  技术问题 → technical (Opus 4.5)          │   │
│  │  创意内容 → creative  (Sonnet 4)          │   │
│  │  记忆操作 → memory    (Haiku)             │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
│  ┌─ 最近调度 ───────────────────────────────┐   │
│  │  14:32  用户→orchestrator→companion  ✓    │   │
│  │  14:28  系统→orchestrator→memory     ✓    │   │
│  │  14:15  cron→orchestrator→creative   ✓    │   │
│  └──────────────────────────────────────────┘   │
│                                                 │
└─────────────────────────────────────────────────┘
```

**交互增强：**
- Agent 拓扑图以 SVG 绘制，节点可拖拽重排
- 节点带状态色（活跃=绿, 空闲=灰, 错误=红）
- 调度链路动画（数据流从入口流向目标 Agent）
- 路由规则支持就地编辑

---

## 5. 样式增强方案

### 5.1 中文字体栈

修改 `base.css` 中的字体变量：

```css
:root {
  /* 正文字体 — 增加中文优先 */
  --font-body:
    "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
    "Space Grotesk", -apple-system, BlinkMacSystemFont,
    "Segoe UI", Roboto, sans-serif;

  /* 展示字体 — 标题/大号文字 */
  --font-display:
    "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
    "Space Grotesk", -apple-system, BlinkMacSystemFont,
    "Segoe UI", Roboto, sans-serif;

  /* 等宽字体 — 不变 */
  --mono:
    "JetBrains Mono", ui-monospace, SFMono-Regular,
    "SF Mono", Menlo, Monaco, Consolas, monospace;
}
```

备选方案：通过 Google Fonts 引入 `Noto Sans SC` 以保证跨平台一致性：
```html
<link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;600;700&display=swap" rel="stylesheet">
```

### 5.2 排版适配

中文排版与英文有差异，需要以下调整：

```css
/* 中文排版优化 */
body {
  letter-spacing: 0;           /* 中文不需要负值 letter-spacing */
  line-height: 1.7;            /* 中文行高需要更大（原 1.55 → 1.7）*/
  word-break: break-all;       /* 中英混排断行优化 */
}

/* 标题字间距调整 */
.page-title,
.card-title,
.config-section-card__title {
  letter-spacing: 0;           /* 中文标题不要负间距 */
}

/* 导航项适配中文宽度 */
.nav-item__text {
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 0.02em;      /* 中文导航微增间距提升可读性 */
}
```

### 5.3 交互动画增强

在现有动画基础上增加以下效果：

```css
/* ① 页面切换过渡 */
@keyframes page-enter {
  from {
    opacity: 0;
    transform: translateX(12px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}

.content > section {
  animation: page-enter 0.3s var(--ease-out) backwards;
}

/* ② 卡片悬浮提升效果 — 增强原有 .card:hover */
.card:hover {
  border-color: var(--border-strong);
  box-shadow: var(--shadow-md), inset 0 1px 0 var(--card-highlight);
  transform: translateY(-2px);     /* 新增：悬浮微升 */
}

/* ③ 数值变化动画 */
@keyframes count-up {
  from { opacity: 0; transform: translateY(8px); }
  to   { opacity: 1; transform: translateY(0); }
}

.stat-value[data-animate] {
  animation: count-up 0.4s var(--ease-out) backwards;
}

/* ④ 状态切换动画（连接/断开） */
@keyframes status-change {
  0%   { transform: scale(1); }
  50%  { transform: scale(1.15); }
  100% { transform: scale(1); }
}

.statusDot.transitioning {
  animation: status-change 0.3s var(--ease-spring);
}

/* ⑤ 列表项交错入场 */
.list-item {
  animation: rise 0.35s var(--ease-out) backwards;
}

.list-item:nth-child(1) { animation-delay: 0ms; }
.list-item:nth-child(2) { animation-delay: 40ms; }
.list-item:nth-child(3) { animation-delay: 80ms; }
.list-item:nth-child(4) { animation-delay: 120ms; }
.list-item:nth-child(5) { animation-delay: 160ms; }

/* ⑥ 按钮点击涟漪效果 */
.btn::after {
  content: '';
  position: absolute;
  inset: 0;
  border-radius: inherit;
  background: currentColor;
  opacity: 0;
  transition: opacity 0.2s;
}

.btn:active::after {
  opacity: 0.08;
}

/* ⑦ Toast 通知动画 */
@keyframes toast-in {
  from {
    opacity: 0;
    transform: translateY(100%) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}

@keyframes toast-out {
  to {
    opacity: 0;
    transform: translateY(100%) scale(0.95);
  }
}

.toast {
  animation: toast-in 0.3s var(--ease-out);
}

.toast.leaving {
  animation: toast-out 0.2s var(--ease-out) forwards;
}
```

### 5.4 主题色方案

保留 OpenClaw 的红色主题（`--accent: #ff5c5c`），额外为 SAVC 专属面板定义辅助色：

```css
:root {
  /* SAVC 专属色 */
  --savc-primary: #ff5c5c;              /* 继承 OpenClaw 主色 */
  --savc-memory: #3b82f6;               /* 记忆系统 — 蓝色 */
  --savc-memory-subtle: rgba(59, 130, 246, 0.12);
  --savc-persona: #a855f7;              /* 人格配置 — 紫色 */
  --savc-persona-subtle: rgba(168, 85, 247, 0.12);
  --savc-orchestrator: #14b8a6;         /* 编排中心 — 青色（复用 accent-2）*/
  --savc-orchestrator-subtle: rgba(20, 184, 166, 0.12);
}
```

---

## 6. 实现计划

### 6.1 阶段划分

```
Phase UI-1: 基础中文化与字体
─────────────────────────────
  ├── M-UI1.1: index.html lang="zh-CN" + 中文字体引入
  ├── M-UI1.2: i18n 模块搭建 (zh-CN.ts + t() 函数)
  ├── M-UI1.3: navigation.ts 中文化（Tab 标签 + 分组名）
  └── M-UI1.4: base.css 字体栈 + 排版参数调整

Phase UI-2: 仪表盘重构
─────────────────────────
  ├── M-UI2.1: dashboard.ts 新视图（替代 overview.ts）
  ├── M-UI2.2: 状态卡片带动画效果
  ├── M-UI2.3: 媛媛状态区（SAVC 专属信息展示）
  └── M-UI2.4: 最近活动列表（实时 WebSocket 推送）

Phase UI-3: SAVC 专属面板
───────────────────────────
  ├── M-UI3.1: memory.ts 记忆系统管理面板
  ├── M-UI3.2: persona.ts 人格配置面板
  ├── M-UI3.3: orchestrator.ts Agent 编排可视化
  └── M-UI3.4: savc.css 专属样式

Phase UI-4: 交互增强
──────────────────────
  ├── M-UI4.1: 页面切换过渡动画
  ├── M-UI4.2: Toast 通知系统（操作反馈）
  ├── M-UI4.3: 列表项交错动画 + 卡片悬浮效果
  └── M-UI4.4: 导航增强（键盘快捷键 + tooltip）

Phase UI-5: 现有视图中文化
──────────────────────────
  ├── M-UI5.1: chat.ts 对话界面中文化
  ├── M-UI5.2: agents.ts Agent 管理中文化
  ├── M-UI5.3: channels.ts 渠道管理中文化
  ├── M-UI5.4: config.ts 系统设置中文化
  ├── M-UI5.5: sessions / cron / logs 中文化
  └── M-UI5.6: 响应式布局优化与移动端触屏适配
```

### 6.2 里程碑与验收标准

| 里程碑 | 验收标准 |
|--------|----------|
| M-UI1 基础中文化 | 导航栏全中文显示；中文排版无毛刺；主题切换正常 |
| M-UI2 仪表盘 | Dashboard 展示所有关键指标；状态卡片动画流畅；中文标签完整 |
| M-UI3 SAVC 面板 | 记忆列表可搜索、可编辑；人格参数可视化调整；Agent 拓扑图可交互 |
| M-UI4 交互增强 | 页面切换有过渡动画；操作有 Toast 反馈；列表入场有交错效果 |
| M-UI5 全量中文化 | 所有视图无英文残留（除技术术语）；移动端可正常操作 |

### 6.3 依赖关系

```
Phase UI-1 (基础) ──→ Phase UI-2 (仪表盘) ──→ Phase UI-4 (交互)
                 └──→ Phase UI-3 (SAVC面板) ──┘
                 └──→ Phase UI-5 (全量中文化) ──┘
```

UI-1 是基础，必须最先完成。UI-2/3/5 可以在 UI-1 之后并行推进。UI-4 作为增强层最后统一落地。

---

## 7. 技术注意事项

### 7.1 不破坏 OpenClaw 上游

重构**必须**以增量方式进行，不直接修改 OpenClaw 子模块中的源文件。策略：

1. **视图文件**：新增文件放在 SAVC 自有目录，通过 `app-render.ts` 的条件分支引入
2. **样式文件**：新增 `savc.css` 在入口文件中 `@import`，不修改原 CSS 文件
3. **i18n**：通过 `t()` 函数包裹所有文本，原文件中的英文文本通过 `t()` 替换为中文

如果需要修改 OpenClaw 源文件（如 `navigation.ts`），应通过 **fork + patch** 的方式，或在 SAVC 项目层面做 overlay 覆盖。

### 7.2 性能约束

| 约束 | 标准 |
|------|------|
| 首屏加载 | < 2s（Vite 打包后 gzip 体积 < 200KB）|
| 页面切换 | < 100ms 感知延迟 |
| 动画帧率 | 60fps（使用 `transform` / `opacity` 而非 `top` / `left`）|
| 中文字体 | 使用系统字体栈，不打包完整中文字体文件 |
| 内存占用 | 记忆列表使用虚拟滚动（> 100 条时）|

### 7.3 与后端 Phase 的协调

| UI 功能 | 依赖的后端能力 | 对应 Phase |
|---------|----------------|-----------|
| 记忆搜索 | `memory.search` RPC | Phase 4a 已完成 |
| 记忆编辑 | `memory.update` / `memory.delete` RPC | 需新增 |
| 人格配置 | 读写 `SOUL.md` / `voice.yaml` | 需新增 Gateway 方法 |
| Agent 拓扑 | `agents.list` + 路由规则查询 | Phase 4b 已完成 |
| 调度日志 | `orchestrator.dispatches` 查询 | 需新增 |
| 实时活动 | WebSocket 事件订阅 | 已有基础设施 |

---

## 8. 附录

### A. 现有 CSS 变量清单（可直接复用）

| 变量 | 用途 | 暗色值 | 亮色值 |
|------|------|--------|--------|
| `--bg` | 页面背景 | `#12141a` | `#fafafa` |
| `--accent` | 主题强调色 | `#ff5c5c` | `#dc2626` |
| `--text` | 正文文字 | `#e4e4e7` | `#3f3f46` |
| `--muted` | 辅助文字 | `#71717a` | `#71717a` |
| `--border` | 边框 | `#27272a` | `#e4e4e7` |
| `--card` | 卡片背景 | `#181b22` | `#ffffff` |
| `--ok` | 成功/正常 | `#22c55e` | `#16a34a` |
| `--warn` | 警告 | `#f59e0b` | `#d97706` |
| `--danger` | 危险/错误 | `#ef4444` | `#dc2626` |
| `--radius-md` | 通用圆角 | `8px` | `8px` |
| `--ease-out` | 缓出曲线 | `cubic-bezier(0.16, 1, 0.3, 1)` | 同左 |
| `--ease-spring` | 弹性曲线 | `cubic-bezier(0.34, 1.56, 0.64, 1)` | 同左 |

### B. 可复用的现有组件

| 组件 | CSS 类名 | 用途 |
|------|----------|------|
| 卡片 | `.card` `.card-title` `.card-sub` | 信息容器 |
| 按钮 | `.btn` `.btn.primary` `.btn.danger` | 操作按钮 |
| 统计值 | `.stat` `.stat-label` `.stat-value` | 数值展示 |
| 状态指示 | `.statusDot` `.statusDot.ok` | 状态圆点 |
| 标签 | `.pill` `.chip` `.chip-ok` `.chip-warn` | 标签/分类 |
| 表单 | `.field` `.form-grid` | 表单布局 |
| 列表 | `.list` `.list-item` | 数据列表 |
| 代码块 | `.code-block` | 代码展示 |
| 提示框 | `.callout` `.callout.danger` `.callout.info` | 消息提示 |
| 分段控件 | `.cfg-segmented` `.cfg-segmented__btn` | 模式切换 |
| 开关 | `.cfg-toggle` `.cfg-toggle-row` | 布尔切换 |
| 输入框 | `.cfg-input` `.cfg-textarea` `.cfg-select` | 表单控件 |

### C. 现有动画清单

| 动画 | 关键帧名 | 用途 |
|------|----------|------|
| 卡片入场 | `rise` | 元素从下方淡入 |
| 淡入 | `fade-in` | 简单透明度过渡 |
| 缩放入场 | `scale-in` | 模态框弹入 |
| 仪表盘入场 | `dashboard-enter` | 整体页面入场 |
| 骨架屏 | `shimmer` | 加载占位符 |
| 脉动 | `pulse-subtle` | 状态指示器 |
| 发光脉动 | `glow-pulse` | 按钮/焦点高亮 |
| 主题切换 | `theme-circle-transition` | 圆形展开切换主题 |
