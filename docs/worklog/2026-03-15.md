## 1) 整理 SAVC 最小必填环境变量表

- 对话目标
  - 在已有“运行时配置生效清单”的基础上，再收敛出一份可直接执行的“最小必填环境变量表”。

- 执行内容
  - 读取 `config/.env.example`、`infra/docker/.env.example`、`infra/docker/.env.prod.example`，整理出本机裸跑、Docker 联调、Docker 生产三条路径各自的最小输入。
  - 对照 `scripts/dev_preflight.sh` 与 `scripts/infra/validate_cloud_env.sh`，把要求区分为：
    - 硬要求
    - 最低可用
    - 按功能启用
  - 更新 `docs/SAVC运行时配置生效清单.md`：
    - 新增“最小必填环境变量表”章节
    - 明确哪些变量是脚本会直接卡住的
    - 明确哪些变量不是硬要求，但缺了基本无法实际使用
    - 明确 Discord / Telegram / Calendar / Weather / Codex ACP 的按功能最小组合
  - 新建当天工作日志并更新索引。

- 修改文件
  - `docs/SAVC运行时配置生效清单.md`
  - `docs/worklog/2026-03-15.md`
  - `docs/worklog/INDEX.md`

- 验证结果
  - `sed -n '1,280p' scripts/dev_preflight.sh`：通过，确认本机开发链硬要求主要是 `config/.env.local` 存在，provider key 目前是 warn 级别。
  - `sed -n '1,260p' scripts/infra/validate_cloud_env.sh`：通过，确认生产链硬要求包括 gateway token、至少一个 provider secret、禁止前端暴露 gateway token。
  - `sed -n '1,260p' docs/SAVC运行时配置生效清单.md`：通过，确认新章节已写入。
  - `rg -n "最小必填环境变量表|OPENCLAW_GATEWAY_TOKEN_FILE|VITE_SAVC_GATEWAY_URL=/gateway|DISCORD_BOT_TOKEN|GOOGLE_CALENDAR_ID" docs/SAVC运行时配置生效清单.md`：通过，确认关键最小项已覆盖。

- 备注
  - 这份表的目标不是替代完整模板，而是回答“我至少填哪些才能把系统拉起来并具备最小可用能力”。

## 2) 提供 Docker 生产环境填写实例

- 对话目标
  - 给出一份可直接复制的 Docker 生产环境填写实例，减少用户从 `.env.prod.example` 手工拼装的成本。

- 执行内容
  - 新增 `infra/docker/.env.prod.sample`，内容采用当前生产链推荐方式：
    - 使用 `*_FILE` 文件型 secrets
    - 以前端 `/gateway` 代理为默认入口
    - 以 Volces 作为最小单 provider 示例
    - 为 Codex ACP、Discord、OpenWeather 提供注释化扩展示例
  - 更新 `infra/docker/README.md`：
    - 在文件列表中加入 `.env.prod.sample`
    - 在生产启动步骤中加入 `cp infra/docker/.env.prod.sample infra/docker/.env.prod`

- 修改文件
  - `infra/docker/.env.prod.sample`
  - `infra/docker/README.md`
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `sed -n '1,220p' infra/docker/.env.prod.sample`：通过，确认样例文件已生成，且未包含真实 secret。
  - `docker compose --env-file infra/docker/.env.prod.sample -f infra/docker/docker-compose.prod.yml config`：通过，确认样例可被 compose 正常展开。
  - `sed -n '1,120p' infra/docker/README.md`：通过，确认 README 已引用该样例文件。

- 备注
  - 该样例是“可提交的占位实例”，不是可直接上线的真实生产配置；上线前仍需写入 `infra/docker/secrets/*` 中的真实 secret 文件。

## 3) 提交并推送未同步的文档与配套文件

- 对话目标
  - 将本轮尚未同步到远端的文档与其直接依赖的仓库文件提交并推送到 `origin/main`。

- 执行内容
  - 检查当前工作区，区分出可推送的文档/配置文件与不应纳入提交的本地运行产物：
    - 纳入提交：`docs/*`、`infra/docker/README.md`、`infra/docker/.env.prod.sample`、`infra/docker/docker-compose.prod.yml`、`infra/nginx/thousancliffs.cloud.conf`
    - 排除提交：`packages/config/storage/*`、`.pnpm-store/` 等运行期产物
  - 创建本地提交：
    - `bd38ac9 docs: add savc deployment references`
  - 首次直接执行 `git push origin main` 时因 DNS 解析失败未成功。
  - 改为提权执行同一推送命令后，成功将提交推送到远端 `origin/main`。

- 修改文件
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `git commit -m "docs: add savc deployment references"`：通过，本地提交成功。
  - `git push origin main`：首次未通过，报错 `Could not resolve hostname github.com: Temporary failure in name resolution`。
  - 提权后再次执行 `git push origin main`：通过，返回 `ada4d08..bd38ac9  main -> main`。

- 备注
  - 本次只推送了文档及其直接依赖的仓库文件，没有带上本地 SQLite/WAL、`.pnpm-store/` 等运行期数据。

## 4) 指导 Docker 生产环境最小配置并核对端口占用

- 对话目标
  - 基于用户选择“火山 provider + 默认端口”，开始指导 Docker 生产环境配置，并先确认默认端口是否会与当前主机上的现有服务冲突。

- 执行内容
  - 读取 `infra/docker/.env.prod.sample` 与 `scripts/infra/prod_container.sh`，基于当前生产链给出最小配置路径：
    - 使用 Volces 作为单一 provider
    - 默认保持 `VITE_SAVC_GATEWAY_URL=/gateway`
    - 默认关闭 `SAVC_CODEX_ACP_ENABLE`
  - 执行本机端口检查，核对 `5174` 与 `18789` 的监听情况。
  - 确认这两个默认生产端口当前都已在本机监听，因此：
    - 若打算在同一台机器上直接启动生产 compose，则会发生端口冲突
    - 若是部署到另一台新机器，才可以继续保留默认端口

- 修改文件
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `ss -ltn '( sport = :5174 or sport = :18789 )'`：通过，确认 `0.0.0.0:5174` 与 `0.0.0.0:18789` 当前均处于监听状态。
  - `sed -n '1,220p' infra/docker/.env.prod.sample`：通过，确认当前生产样例默认使用 Volces 单 provider、`/gateway` 代理和文件型 secrets。
  - `sed -n '1,220p' scripts/infra/prod_container.sh`：通过，确认生产启动链为 `init-env -> validate -> up`。

- 备注
  - 端口是否需要修改，取决于生产是否与现有服务部署在同一台主机；如果是同机部署，必须调整 `OPENCLAW_GATEWAY_PORT` 和 `SAVC_UI_PORT`，否则容器无法绑定端口。

## 5) 确认当前端口由现有 SAVC 容器占用及可复用条件

- 对话目标
  - 回答用户关于“当前监听的 `5174` / `18789` 是否就是现有项目容器端口、是否可以直接复用”的问题。

- 执行内容
  - 读取当前 Docker 运行状态，确认：
    - `5174` 由 `savc-cloud-savc-ui-1` 占用
    - `18789` 由 `savc-cloud-savc-gateway-1` 占用
  - 明确区分两种场景：
    - 若生产 compose 要与当前 `savc-cloud` 栈同时运行在同一台机器上，则不能复用同一个宿主机端口
    - 若生产 compose 是用来替换当前 `savc-cloud` 栈，则可以先停掉现有栈，再继续使用同一组宿主机端口

- 修改文件
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `docker ps --format 'table {{.Names}}\t{{.Image}}\t{{.Ports}}'`：通过，确认 `savc-cloud-savc-ui-1` 绑定 `0.0.0.0:5174->5174/tcp`，`savc-cloud-savc-gateway-1` 绑定 `0.0.0.0:18789->18789/tcp`。
  - `bash scripts/infra/dev_container.sh ps`：通过，确认当前运行中的 `savc-cloud` 栈为 `savc-gateway` healthy、`savc-ui` healthy，并分别占用 `18789` 与 `5174`。

- 备注
  - “可复用”只成立于停旧起新，不成立于两套栈并行共存。

## 6) 预配置 Docker 生产环境并修正生产校验链

- 对话目标
  - 按“停旧起新 + 火山单 provider + 默认端口”的路径，先把不涉及用户密钥的生产配置准备好，并清理阻塞生产校验的脚本问题。

- 执行内容
  - 检查 `infra/docker/` 当前状态，确认：
    - `infra/docker/.env.prod` 尚不存在
    - `infra/docker/secrets/` 尚不存在
  - 基于样例文件创建本地生产配置：
    - `cp infra/docker/.env.prod.sample infra/docker/.env.prod`
    - `mkdir -p infra/docker/secrets`
  - 生成本机使用的 gateway token 文件：
    - `infra/docker/secrets/openclaw_gateway_token`
  - 发现并修复 `scripts/infra/prod_container.sh` 的两个问题：
    - 帮助文案仍引用旧路径 `scripts/prod_container.sh`
    - `validate_env()` 错误调用了不存在的 `scripts/validate_cloud_env.sh`
  - 发现并修复 `scripts/infra/validate_cloud_env.sh` 的两个问题：
    - 对 `./secrets`、`./bootstrap/...` 的相对路径解析与 compose 不一致
    - 对 provider secret 只检查变量是否声明，不检查对应文件是否真实存在
  - 重新执行生产校验，最终将阻塞项收敛为唯一缺口：
    - 尚未提供真实的 `VOLCES_API_KEY` secret 文件

- 修改文件
  - `scripts/infra/prod_container.sh`
  - `scripts/infra/validate_cloud_env.sh`
  - `docs/worklog/2026-03-15.md`
  - `infra/docker/.env.prod`（本地 gitignored 配置文件）
  - `infra/docker/secrets/openclaw_gateway_token`（本地 secret 文件）

- 验证结果
  - `ls -la infra/docker`：通过，确认 `.env.prod.sample` 已存在，`.env.prod` 之前缺失。
  - `cp infra/docker/.env.prod.sample infra/docker/.env.prod && mkdir -p infra/docker/secrets ...`：通过，确认本地生产配置文件和 gateway token 文件已创建。
  - 首次 `bash scripts/infra/prod_container.sh validate`：未通过，报错找不到 `scripts/validate_cloud_env.sh`。
  - 修复 `scripts/infra/prod_container.sh` 后再次校验：未通过，暴露出相对路径解析和 provider secret 校验过宽的问题。
  - 修复 `scripts/infra/validate_cloud_env.sh` 后再次校验：未通过，结果准确收敛为 `no LLM provider secret configured`。
  - `bash scripts/infra/prod_container.sh validate` 最终输出：`PASS 6 / WARN 2 / FAIL 1`，仅剩 provider secret 未配置。

- 备注
  - 当前生产切换前唯一缺的是真实的 `infra/docker/secrets/volces_api_key` 文件；补上后即可再次校验，并进入停旧起新的启动步骤。

## 7) 修复生产 Docker 启动链并完成停旧起新验证

- 对话目标
  - 在用户已补齐生产 secret 后，直接修复 `docker` 生产栈启动失败问题，并把 `savc-gateway` / `savc-ui` 以默认端口成功拉起。

- 执行内容
  - 接手检查此前失败的 `bash scripts/infra/prod_container.sh up` 会话，确认阻塞点不再是 `.env.prod` 或 secrets，而是生产镜像在构建阶段试图编译 vendored `openclaw/` 快照。
  - 静态核对 `openclaw` 生产构建入口与源码树，确认当前 vendored 快照缺失大量生产入口依赖文件，不适合作为 Docker 镜像内现编来源。
  - 将生产镜像调整为“只复制仓库源码，不在 build 阶段编译 `openclaw/`”，并把运行时 `OPENCLAW_ROOT` 统一切到 pnpm 安装后的稳定路径 `/workspace/node_modules/.pnpm/node_modules/openclaw`。
  - 在 `infra/docker/docker-compose.prod.yml` 中为 `savc-gateway`、`savc-ui`、`savc-proactive` 增加首次启动时的懒安装逻辑：
    - 若 `OPENCLAW_ROOT/openclaw.mjs` 不存在，则执行 `pnpm --dir /workspace install --no-frozen-lockfile --lockfile=false`
    - `savc-gateway` 与 `savc-proactive` 在执行 `scripts/setup.sh` 后，再显式回灌 `~/.openclaw/.env` 到当前 shell，确保 `*_FILE` secret 解析后的环境变量能被最终 `node` 进程继承
    - `savc-ui` 在启动前显式解析 `OPENCLAW_GATEWAY_TOKEN`
  - 修正生产网关健康检查路径为 `/healthz`，与当前 OpenClaw Gateway 实际健康接口保持一致。
  - 重新执行停旧起新：
    - `bash scripts/infra/prod_container.sh down`
    - `bash scripts/infra/prod_container.sh up`
  - 首次重启后发现 `savc-gateway` 因 `OPENCLAW_GATEWAY_TOKEN` 未从子 shell 回传而 unhealthy；补上 `.openclaw/.env` 回灌后再次 `down/up`，最终 `savc-gateway` healthy，随后再次 `up` 让 `savc-ui` 在健康网关基础上成功启动。

- 修改文件
  - `infra/docker/Dockerfile.gateway`
  - `infra/docker/Dockerfile.runtime`
  - `infra/docker/docker-compose.prod.yml`
  - `infra/docker/README.md`
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `bash scripts/infra/prod_container.sh validate`：通过，结果 `PASS 7 / WARN 2 / FAIL 0`。
  - `bash scripts/infra/prod_container.sh config`：通过，确认生产 compose 可正常展开，`OPENCLAW_ROOT` 已指向 `/workspace/node_modules/.pnpm/node_modules/openclaw`。
  - `bash scripts/infra/prod_container.sh up`：首次未通过，定位到 `savc-gateway` 健康检查前 `OPENCLAW_GATEWAY_TOKEN` 未传递给最终网关进程。
  - 修复命令块后再次 `bash scripts/infra/prod_container.sh down && bash scripts/infra/prod_container.sh up`：通过，`savc-gateway` / `savc-ui` 均成功创建。
  - `docker inspect --format '{{json .State.Health}}' savc-prod-savc-gateway-1`：通过，最终状态 `healthy`。
  - `docker inspect --format '{{json .State.Health}}' savc-prod-savc-ui-1`：通过，最终状态 `healthy`。
  - `bash scripts/infra/prod_container.sh ps`：通过，确认：
    - `savc-gateway` 运行于 `0.0.0.0:18789->18789/tcp`
    - `savc-ui` 运行于 `0.0.0.0:5174->5174/tcp`
  - 主机侧端口检查：
    - `curl -i http://127.0.0.1:18789/healthz`：通过，返回 `HTTP/1.1 200 OK` 与 `{"ok":true,"status":"live"}`
    - `curl -I http://127.0.0.1:5174`：通过，返回 `HTTP/1.1 200 OK`

- 备注
  - 当前生产栈已成功切换为“镜像内复制源码 + 容器首次启动懒安装 npm openclaw”的路径，绕开了 vendored `openclaw/` 快照在 Docker build 阶段无法现编的问题。
  - 本轮未改动用户运行时 SQLite/WAL/YAML 数据文件，只保留了容器运行带来的正常运行期更新。

## 8) 核对 Discord 生效状态与宿主机 Nginx 反向代理

- 对话目标
  - 确认当前生产环境里 Discord 配置是否已经实际生效，并核对域名是否仍然通过宿主机 Nginx 反向代理到新起的 Docker 服务。

- 执行内容
  - 检查 `infra/docker/.env.prod` 中与生产运行相关的 Discord 环境项，确认 `DISCORD_BOT_TOKEN_FILE` 当前为空。
  - 进入运行中的 `savc-gateway` 容器，核对生成后的 `~/.openclaw/openclaw.json` 与 `~/.openclaw/.env`：
    - `channels.discord.enabled` 当前为 `false`
    - `~/.openclaw/.env` 中未写入 `DISCORD_BOT_TOKEN`
  - 对比插件层配置，确认 `plugins.entries.discord.enabled = true` 只是插件入口开启，不代表 Discord Bot 通道已启用。
  - 检查宿主机当前加载中的 Nginx 配置与监听状态，确认：
    - `thousancliffs.cloud` / `www.thousancliffs.cloud` 代理到 `127.0.0.1:5174`
    - `gateway.thousancliffs.cloud` 代理到 `127.0.0.1:18789`
    - `manage.thousancliffs.cloud` 代理到 `127.0.0.1:8317`
    - 宿主机当前正在监听 `0.0.0.0:80`

- 修改文件
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `grep -E '^(DISCORD_BOT_TOKEN|DISCORD_BOT_TOKEN_FILE|OPENCLAW_GATEWAY_PORT|SAVC_UI_PORT|VITE_SAVC_GATEWAY_URL|SAVC_GATEWAY_INTERNAL_URL)=' infra/docker/.env.prod`：通过，确认 `DISCORD_BOT_TOKEN_FILE=` 当前为空。
  - `docker exec savc-prod-savc-gateway-1 sh -lc "grep -n '\"discord\"' -A12 -B4 /home/node/.openclaw/openclaw.json"`：通过，确认运行态 `channels.discord.enabled` 为 `false`。
  - `docker exec savc-prod-savc-gateway-1 sh -lc "grep -n 'DISCORD_BOT_TOKEN' /home/node/.openclaw/.env || true"`：通过，确认运行态未注入 Discord token。
  - `nginx -T | rg 'thousancliffs\\.cloud|gateway\\.thousancliffs\\.cloud|proxy_pass http://127\\.0\\.0\\.1:(5174|18789)'`：通过，确认域名反代配置已被宿主机 Nginx 实际加载。
  - `ss -ltn '( sport = :80 )'`：通过，确认宿主机当前监听 80 端口。

- 备注
  - 当前域名反代是生效的，但 Docker 仍把 `5174` / `18789` 直接发布到了 `0.0.0.0`；如果希望只能经由 Nginx 访问，后续应把 compose 端口绑定收紧到 `127.0.0.1`。

## 9) 预接入 Discord / Telegram 生产 secret 文件位

- 对话目标
  - 先把生产环境中 Discord 与 Telegram 的 token 文件位和 `.env.prod` 接线准备好，缩短后续真正启用这两个通道时的改动范围。

- 执行内容
  - 检查当前生产配置，确认：
    - `infra/docker/.env.prod` 中 `DISCORD_BOT_TOKEN_FILE` 与 `TELEGRAM_BOT_TOKEN_FILE` 为空
    - `infra/docker/secrets/` 目录下尚无对应 token 文件
  - 更新本地生产环境文件 `infra/docker/.env.prod`：
    - `DISCORD_BOT_TOKEN_FILE=/run/savc-secrets/discord_bot_token`
    - `TELEGRAM_BOT_TOKEN_FILE=/run/savc-secrets/telegram_bot_token`
  - 创建占位 secret 文件：
    - `infra/docker/secrets/discord_bot_token`
    - `infra/docker/secrets/telegram_bot_token`
  - 收紧两个占位 secret 文件权限到 `0600`
  - 重新执行生产校验，确认在接入新的 secret 文件位后，现有 Docker 生产配置仍然可以正常通过校验

- 修改文件
  - `infra/docker/.env.prod`
  - `infra/docker/secrets/discord_bot_token`
  - `infra/docker/secrets/telegram_bot_token`
  - `docs/worklog/2026-03-15.md`

- 验证结果
  - `grep -E '^(DISCORD_BOT_TOKEN_FILE|TELEGRAM_BOT_TOKEN_FILE)=' infra/docker/.env.prod`：通过，确认两项都已指向 `/run/savc-secrets/*`
  - `ls -l infra/docker/secrets`：通过，确认两个 token 文件已创建，权限为 `-rw-------`
  - `bash scripts/infra/prod_container.sh validate`：通过，结果 `PASS 7 / WARN 2 / FAIL 0`

- 备注
  - 这一步只完成了文件位预接入；由于两个新 secret 文件当前仍是占位内容，Discord / Telegram 运行时通道还不会真正启用。
  - 后续只需把真实 token 写入这两个文件，再重启生产栈，即可让 `scripts/setup.sh` 把对应通道切到 enabled。
