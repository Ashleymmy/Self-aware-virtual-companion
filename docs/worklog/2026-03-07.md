# 2026-03-07

## 记录 1

- 对话目标：审查 `docs/openclaw-customPrmopt` 目录中的 skills、agent/usecase 模板与其他提示词内容，筛出一份“质量较高且未发现恶意脚本注入迹象”的保守清单。
- 执行内容：盘点 `openclaw-customPrmopt` 下各子仓库与文件类型；对 `awesome-openclaw-skills-6d998.../skills` 执行静态风险检索，重点检查 `curl|bash`、`base64` 解码执行、反弹 shell、破坏性命令与远程安装器；定位并确认 `bybit-trading` 存在恶意远程执行链；抽查 `Awesome-Prompts-master` 中实用工具/元提示词内容质量；抽查 `awesome-openclaw-agents` 的 SOUL 模板与 `awesome-openclaw-usecases*` 的 usecase 文档；使用仓库内 `skill-scanner` 对候选 skills 做二次静态复核；整理出保守白名单并写入专门清单文档。
- 修改文件：`docs/openclaw-customPrmopt/SAFE_SHORTLIST.md`、`docs/worklog/INDEX.md`、`docs/worklog/2026-03-07.md`
- 验证结果：执行多轮 `rg` 静态检索未在 `Awesome-Prompts-master`、`awesome-openclaw-agents`、`awesome-openclaw-usecases*` 中发现 `curl|bash`、`base64|bash`、`/dev/tcp`、`nc -e` 等典型恶意模式；使用 `skill-scanner.py` 复核后，`github-pr`、`tmux`、`test-gen`、`github-kb`、`reflect`、`jina-reader`、`reddit-2`、`searxng-2` 通过，`bybit-trading` 被确认存在高风险远程执行命令；已生成 `docs/openclaw-customPrmopt/SAFE_SHORTLIST.md` 作为本轮输出。
- 备注：`awesome-openclaw-skills-cebab...` 仅为外链索引仓库，`skills-727f9e33...zip` 为未全量审计的大型压缩包，本轮均未纳入白名单。

## 记录 2

- 对话目标：梳理当前仓库里与系统提示词相关的编辑入口，并说明实际运行时的执行链路与优先级。
- 执行内容：核对仓库内 `savc-core/*`、`config/openclaw/agents/*/SOUL.md`、`scripts/setup.sh`、`openclaw` 运行时代码和当前本机 `/home/min/.openclaw/openclaw.json`；确认当前 live 配置的 `agents.defaults.workspace` 与各专家 agent 的 `workspace` 都指向仓库内 `savc-core`，因此运行时注入的 workspace bootstrap 文件来自 `savc-core/AGENTS.md`、`SOUL.md`、`TOOLS.md`、`IDENTITY.md`、`USER.md`、`HEARTBEAT.md`；同时确认 Telegram/Discord 的额外 `systemPrompt` 配置位存在于 OpenClaw 运行时配置，但当前实例未设置；整理出 setup 模板层、workspace 注入层、运行时拼接层和 hook/渠道附加层的关系。
- 修改文件：`docs/worklog/2026-03-07.md`
- 验证结果：已核对 `/home/min/.openclaw/openclaw.json`，确认 `agents.defaults.workspace` 为 `/home/min/SAVC/Self-aware-virtual-companion/savc-core`，各专家 agent `workspace` 同样指向该目录；代码层已确认 `resolveBootstrapContextForRun()` 会读取 workspace bootstrap files，`buildEmbeddedSystemPrompt()`/`buildAgentSystemPrompt()` 负责拼接最终系统提示词，`applySystemPromptOverrideToSession()` 会将拼好的 prompt 覆盖到 session；当前运行时未发现 Telegram 群组或 Discord 频道级 `systemPrompt` 已配置，heartbeat prompt 也未在配置中单独覆写。
- 备注：`config/openclaw/agents/*/SOUL.md` 仍会在 setup 阶段复制到 `~/.openclaw/agents/*/SOUL.md`，但当前主链路里更直接影响最终 prompt 的是 `savc-core` 工作区文件与 OpenClaw runtime builder。

## 记录 3

- 对话目标：将当前系统提示词编辑入口与生效方式整理成简明对照表，便于后续直接修改。
- 执行内容：基于已确认的 live 配置与运行时代码，提炼出“编辑位置 / 作用 / 当前是否直接生效 / 是否需要重跑 setup / 备注”的对照关系，并准备面向用户的简版说明。
- 修改文件：`docs/worklog/2026-03-07.md`
- 验证结果：复用上一轮已核对的 `/home/min/.openclaw/openclaw.json` 与 `openclaw/src/agents/*` 代码结论；当前系统提示词主入口仍为 `savc-core/*`，渠道级 `systemPrompt` 仍未发现已配置项。
- 备注：本轮无业务代码改动，仅补充说明与日志。
