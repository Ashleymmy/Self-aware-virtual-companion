# 2026-03-13

## 1) 核对 OpenClaw 源码是否曾被修改

- 对话目标
  - 确认仓库历史中是否改动过 `openclaw/` 源码，并区分历史改动与当前未提交改动。

- 执行内容
  - 检查 `git log -- openclaw`，确认历史上确实存在多次涉及 `openclaw/` 的提交。
  - 核对 `b4d6a04d2 chore(vendor): refresh openclaw snapshot` 等提交，确认此前做过 OpenClaw vendor 快照更新与相关源码变更。
  - 检查当前工作区状态，确认本轮未提交改动不包含 `openclaw/` 目录。

- 修改文件
  - `docs/worklog/2026-03-13.md`
  - `docs/worklog/INDEX.md`

- 验证结果
  - `git log --oneline -- openclaw`：通过，确认存在历史 OpenClaw 改动记录
  - `git show --stat --name-only b4d6a04d2 -- openclaw`：通过，确认存在 OpenClaw vendor 快照更新
  - `git status --short`：通过，当前未提交改动不含 `openclaw/`

- 备注
  - 结论是“历史上改过，但当前这一轮未提交改动没有继续动 `openclaw/`”。

## 2) 查看 `docs/兼容` 目录文件

- 对话目标
  - 检查 `docs/兼容` 目录下有哪些文件，并概括其内容范围。

- 执行内容
  - 列出 `docs/兼容` 目录文件，确认当前仅有 `backend-development.md`。
  - 阅读文档开头与章节结构，提取文档定位、技术栈说明与模块目录。
  - 统计文档行数并核对一级、二级标题，确认内容覆盖系统架构、数据库设计、业务模块、安全、部署与分阶段开发计划。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `rg --files docs/兼容`：通过，确认目录内当前只有 1 个文件
  - `wc -l docs/兼容/backend-development.md`：通过，文档共 1835 行
  - `rg -n '^## ' docs/兼容/backend-development.md`：通过，确认共有 20 个一级章节

- 备注
  - 文档顶部已明确说明：2026-03 起后端方案以 `uv + Python 3.12 + FastAPI` 为准，历史 Node 风格片段仅保留业务语义参考。

## 3) 评估用现有项目承接 OpenClaw 集群

- 对话目标
  - 判断是否应继续沿用当前仓库架构来实现 OpenClaw 集群能力，并明确集群化应落在哪一层。

- 执行内容
  - 检查 `docs/separation/openclaw-interface.md`、`docs/separation/architecture.md`，确认当前仓库已形成 `packages/core + packages/plugin + packages/ui + openclaw vendor` 的清晰边界。
  - 检查 `packages/plugin/src/tool-spawn-expert.ts`、`real-session-adapter.ts`、`config.ts`、`types.ts`，确认真实执行链当前仅区分 `mock` / `real`，尚未抽象出 `cluster` 运行模式。
  - 检查 `infra/docker/docker-compose.cloud.yml` 与 `packages/ui` 的 Gateway / task-runtime 相关代码，确认前端和容器层已经具备节点视图、任务运行态和多服务编排基础，但缺少多网关/多节点调度层。
  - 基于当前实现判断：最稳妥方案是在现有项目内新增“集群调度适配层”，而不是另起一套后端重做 OpenClaw 集群。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 代码阅读验证，未执行额外自动化测试。

- 备注
  - 当前仓库更像“单网关 + 编排插件 + 管理 UI”基线；集群化的关键缺口在运行模式扩展、节点注册/选路、任务状态汇总，不在 UI 或 Agent 定义层。

## 4) 明确 Web 后端与 OpenClaw 集群的职责拆分

- 对话目标
  - 根据用户补充，确认 `docs/兼容/backend-development.md` 中后端主要用于支撑 Web 站点业务，而非直接承载 OpenClaw 集群运行时。

- 执行内容
  - 基于用户补充重新收口架构结论：文档内后端负责站点业务域，如用户、商品、订单、工单、支付、通知、文件与 Web 端接口。
  - 明确当前仓库更适合承接 OpenClaw 集群、Agent 编排、节点管理与运行态可视化。
  - 形成拆分建议：Web 业务后端与 OpenClaw 集群保持独立，通过内部 API、任务投递或状态回调进行集成。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 未执行自动化验证；本轮为架构职责澄清。

- 备注
  - 推荐避免把站点业务后端与 Agent 集群运行时混成单一服务，减少耦合与后续演进风险。

## 5) 通读 `docs/兼容` 三份前后端文档并重建整体架构

- 对话目标
  - 完整阅读 `docs/兼容` 下的前端、后端、API 契约文档，结合“OpenClaw 部署在云上”的补充，先厘清整体系统架构，再去判断当前 OpenClaw 项目应该怎么承接。

- 执行内容
  - 重新检查 `docs/兼容` 目录，确认当前共有三份文档：`frontend-development.md`、`backend-development.md`、`api-reference.md`。
  - 分块通读三份文档全文，确认站点整体目标架构为：闲鱼/站点订单入口 -> Web 业务后端 -> OpenClaw 云端 Agent 集群 + 本地 LLM 分析服务 -> Web 前端协作页面。
  - 提炼前端职责：用户侧门户、卡密提取、技术服务项目页、通知中心，以及可选的 `/admin` 后台入口。
  - 提炼后端职责：用户/商品/订单/支付/卡密/工单/项目/通知/WebSocket 中枢，以及对闲鱼、OpenClaw、Local LLM 的外部集成。
  - 提炼 API 契约：前端只依赖 REST + WebSocket；OpenClaw 通过 `OPENCLAW_API_URL` 风格的任务接口与回调接口集成；Agent 回调与 WebSocket 事件是项目协作链路关键。
  - 对照当前仓库现状，检查 `infra/docker/docker-compose.prod.yml`、`scripts/setup.sh`、`packages/plugin/index.ts`、`packages/plugin/openclaw.plugin.json`，确认当前 SAVC 仓库已经具备云上 OpenClaw Gateway + 插件 + 管理 UI 的运行基线，但当前接口形态仍偏 OpenClaw Gateway / plugin tools，而不是文档中假定的独立 `/tasks` REST 服务。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `rg --files docs/兼容`：通过，确认目录内共 3 个文档
  - `wc -l docs/兼容/*.md`：通过，三份文档共 6263 行
  - 完成全文阅读与当前仓库关键云部署文件比对，未执行自动化测试

- 备注
  - 当前最关键的架构认知是：现有 SAVC 仓库对应“云上 OpenClaw 侧”，不是站点业务后端本体；后续研究重点应放在如何让当前 OpenClaw 项目对齐站点后端期待的任务接口与回调模型。

## 6) 列出站点后端期望的 OpenClaw 接口与当前仓库映射

- 对话目标
  - 按清单形式列出兼容文档中站点后端所期待的 OpenClaw 能力，并对应当前 SAVC/OpenClaw 仓库已经具备的部分、缺失部分和建议承接方式。

- 执行内容
  - 基于 `docs/兼容/backend-development.md` 与 `docs/兼容/api-reference.md` 提炼站点后端视角下的 OpenClaw 接口诉求：任务创建、状态查询、消息转发、进度回调、计划回调、完成回调。
  - 结合 `packages/plugin`、`infra/docker`、`scripts/setup.sh` 的当前实现，整理现有仓库可直接复用的能力：专家任务分发、状态查询、运行时部署、插件工具注册、云端 gateway 基线。
  - 输出面向后续设计的三类清单：已具备、需要补齐、建议对外暴露方式。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 未执行自动化验证；本轮为架构映射整理。

- 备注
  - 该清单的目的不是立即改代码，而是避免把“站点后端期待的 REST 任务接口”和“当前 OpenClaw 插件工具接口”混为一谈。

## 7) 明确将商用服务适配作为媛媛的一项能力

- 对话目标
  - 根据用户补充，确认“商用服务适配”应作为媛媛（当前 OpenClaw / SAVC）的可调用能力来实现，并同时为自我迭代、工具学习和反思沉淀结构化数据。

- 执行内容
  - 检查当前仓库中与学习和记忆相关的落点，包括 `packages/core/skills/tool-learner/SKILL.md`、`packages/core/skills/self-reflection/SKILL.md`、`packages/core/memory/tools/learning-queue.md`、`packages/core/memory/procedural/workflows.md`。
  - 结合用户意图收口架构结论：商用服务相关能力应落在 SAVC/OpenClaw 的能力层、工具层或服务适配层，而不是混入站点业务后端或直接写成人格记忆。
  - 形成实践建议：运行时保留结构化任务/结果数据，自我反思与工具学习只消费经过脱敏和归纳的数据，避免将原始订单、支付、密钥等敏感业务数据直接写入长期记忆。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 代码与文档阅读验证，未执行自动化测试。

- 备注
  - 该方向有利于后续形成“能力 -> 执行日志 -> 复盘 -> 工作流固化”的闭环，但前提是把业务遥测、程序记忆、人格记忆严格分层。

## 8) 确认商用业务适配也是对媛媛成熟度的反向补课

- 对话目标
  - 根据用户补充，明确本轮商用业务适配不仅是“服务业务上线准备”，也是在当前缺少真实生产流量、测试环境不完整的情况下，为媛媛项目补齐成熟度验证路径。

- 执行内容
  - 结合用户说明收口当前项目阶段判断：现有媛媛/SAVC 虽然模块较多，但仍处于“浅层可演示、未充分经受真实业务验证”的阶段，不能简单视为成熟项目。
  - 明确本轮商用业务适配的额外价值：通过订单、项目、任务协作、回调、异常处理、人工修正等业务链路，倒逼能力边界、数据结构、运行闭环和学习闭环更完整。
  - 形成当前阶段定位：该适配工作既是业务对接，也是为媛媛提供接近真实生产的“半受控训练场”，用于积累能力演化所需的数据和反馈。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 未执行自动化验证；本轮为架构定位澄清。

- 备注
  - 关键前提仍是先把运行记录、回放、失败归因、人工介入与脱敏数据沉淀机制设计好，否则业务接入只能带来复杂度，不能稳定转化为成熟度提升。

## 9) 提升商用逻辑打通的优先级

- 对话目标
  - 根据用户补充，确认当前阶段应优先打通商用业务链路，以支撑后续测试与上线准备；成熟度建设与学习闭环继续保留，但不应压过主业务交付优先级。

- 执行内容
  - 收口当前阶段优先级判断：以“商用逻辑先跑通”为第一目标，把媛媛的成熟化收益视为本轮工作的次级增益，而不是主要交付物。
  - 明确后续排序应偏向：业务接口、任务流转、状态回传、人工兜底、联调验证；学习与反思相关设计只保留必要埋点，不抢主线资源。
  - 形成阶段性原则：先让商用链路可测试、可回放、可上线，再逐步把运行数据接入更深的学习和自我迭代模块。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 未执行自动化验证；本轮为优先级调整。

- 备注
  - 当前最重要的是避免“为了未来智能化而延迟当前业务闭环”；业务通路应作为第一里程碑。

## 10) 输出商用适配清单与开发文档到 `docs/兼容`

- 对话目标
  - 根据用户要求，将面向当前阶段的 OpenClaw 商用适配“具体清单”和“开发文档”输出到 `docs/兼容` 目录，供后续统一查看与迭代。

- 执行内容
  - 新增 `docs/兼容/openclaw-commercial-checklist.md`，将当前阶段商用链路拆分为 `P0 / P1 / P2` 三档，明确联调前必须完成、上线前建议完成、上线后再做的事项。
  - 新增 `docs/兼容/openclaw-commercial-development.md`，基于当前 SAVC/OpenClaw 仓库现状，说明其在整体系统中的角色、建议新增的商用适配服务层、任务接口模型、回调模型、最小复盘设计与分阶段实施建议。
  - 核对新增文档内容与目录落点，确保与现有 `frontend / backend / api-reference` 三份兼容文档形成同一组参考资料。

- 修改文件
  - `docs/兼容/openclaw-commercial-checklist.md`
  - `docs/兼容/openclaw-commercial-development.md`
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `ls -la docs/兼容`：通过，确认新增 2 份文档已写入目录
  - `sed -n '1,260p' docs/兼容/openclaw-commercial-checklist.md`：通过，确认标题、目录与 P0/P1/P2 清单内容正常
  - `sed -n '1,320p' docs/兼容/openclaw-commercial-development.md`：通过，确认架构、接口与模块划分内容正常

- 备注
  - 本轮文档重点是“先打通商用链路”，学习闭环仅保留最低必要埋点，不压过当前主业务优先级。

## 11) 输出 OpenClaw 商用适配 API 接口文档到 `docs/兼容`

- 对话目标
  - 根据用户要求，为商用项目后端开发协作者补充一份独立的 OpenClaw 商用适配 API 接口文档，便于直接按 REST / Webhook 契约对接。

- 执行内容
  - 新增 `docs/兼容/openclaw-commercial-api-reference.md`，沿用现有 `docs/兼容/api-reference.md` 的写法，定义了：
    - 通用规范与鉴权头
    - `GET /health`
    - `POST /tasks`
    - `GET /tasks`
    - `GET /tasks/:id`
    - `POST /tasks/:id/messages`
    - `GET /tasks/:id/events`
    - `POST /tasks/:id/cancel`
    - `POST /tasks/:id/retry`
    - 回调请求头、签名算法、通用回调结构
    - `analysis / plan / progress / complete / message / task.status` 回调事件载荷
    - 状态/任务类型枚举与错误码表
  - 同步在 `docs/兼容/openclaw-commercial-development.md` 顶部补充对该接口文档的引用，便于后续从开发方案跳转到具体接口契约。
  - 核对新增 API 文档的标题、一级章节与目录落点，确认已进入 `docs/兼容` 文档组。

- 修改文件
  - `docs/兼容/openclaw-commercial-api-reference.md`
  - `docs/兼容/openclaw-commercial-development.md`
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `ls -la docs/兼容`：通过，确认新增 API 文档已写入目录
  - `rg -n '^## ' docs/兼容/openclaw-commercial-api-reference.md`：通过，确认共有 10 个一级章节
  - `sed -n '1,260p' docs/兼容/openclaw-commercial-api-reference.md`：通过，确认文档头部、通用规范与任务接口章节内容正常

- 备注
  - 该文档面向“业务后端协作者”，刻意屏蔽了当前仓库内部 `savc_*` 工具与 Session 细节，避免对接层耦合 OpenClaw 内部实现。

## 12) 将 OpenClaw 商用 API 文档收紧为 V1 最小对接约定

- 对话目标
  - 根据用户确认，将商用适配 API 文档进一步收紧为“第一版联调必用字段”视角，降低后端协作者的首轮实现负担。

- 执行内容
  - 在 `docs/兼容/openclaw-commercial-api-reference.md` 的通用规范中新增“1.6 V1 最小对接约定”，明确：
    - V1 必须实现的接口
    - 强烈建议实现的接口
    - 可延后接口
    - 预留接口
    - `POST /tasks` 最小必填字段
    - 推荐附带字段
    - 暂不要求实现的字段
    - V1 回调最小闭环
  - 更新 `POST /tasks`、`POST /tasks/:id/messages` 的字段说明，增加 `V1 要求` 维度。
  - 在 `GET /tasks`、`GET /tasks/:id/events`、`POST /tasks/:id/cancel` 等章节中标注“第一版可延后”或“强烈建议实现”。
  - 在各类回调事件下补充 `V1 最小 payload 字段`，让协作者优先实现必需结构。
  - 在 `docs/兼容/openclaw-commercial-development.md` 顶部补充说明，明确首轮联调字段范围以接口文档中的 V1 最小约定为准。

- 修改文件
  - `docs/兼容/openclaw-commercial-api-reference.md`
  - `docs/兼容/openclaw-commercial-development.md`
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `sed -n '1,260p' docs/兼容/openclaw-commercial-api-reference.md`：通过，确认已新增 `1.6 V1 最小对接约定`
  - `rg -n 'V1 最小对接约定|V1 最小请求体示例|第一版可延后|第一版强烈建议实现' docs/兼容/openclaw-commercial-api-reference.md docs/兼容/openclaw-commercial-development.md`：通过，确认 V1 收口标记已写入相关文档
  - `sed -n '1,40p' docs/兼容/openclaw-commercial-development.md`：通过，确认开发文档顶部已增加 V1 最小约定引用

- 备注
  - 现在这份 API 文档已经同时具备“完整契约”和“首轮最小实现路径”两层阅读方式，更适合直接给协作者落地。

## 13) 确认文档阶段先收口，准备切入开发

- 对话目标
  - 根据用户确认，先冻结当前兼容文档基线，后续进入商用适配开发阶段。

- 执行内容
  - 将当前 `docs/兼容` 文档组视为本阶段对齐基线，不再继续扩展文档范围。
  - 明确后续工作重心切换为实际开发，优先实现商用适配的 P0 最小链路。
  - 收口当前推荐实现起点：服务骨架、`GET /health`、`POST /tasks`、`GET /tasks/:id`、`POST /tasks/:id/messages`。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - 未执行自动化验证；本轮为阶段切换确认。

- 备注
  - 当前文档已经足够支撑协作者对接，后续应优先把接口契约转化为可运行服务。

## 14) 启动 OpenClaw 商用适配服务骨架开发

- 对话目标
  - 根据用户确认，正式开始实现面向商用后端联调的 OpenClaw 适配服务 P0 骨架。

- 执行内容
  - 新增 `packages/service`，作为独立的商用适配服务目录。
  - 实现基础配置加载，支持服务监听地址、基础路径、API Key 与本地数据目录。
  - 实现文件持久化任务存储，支持：
    - 创建任务
    - 查询任务
    - 查询事件流
    - 任务消息追加
    - 任务取消
  - 实现最小 HTTP 服务，提供：
    - `GET /health`
    - `POST /tasks`
    - `GET /tasks`
    - `GET /tasks/:id`
    - `GET /tasks/:id/events`
    - `POST /tasks/:id/messages`
    - `POST /tasks/:id/cancel`
    - `POST /tasks/:id/retry`（当前返回未实现）
  - 为任务创建与存储逻辑补充 Node 原生测试。
  - 在根目录 `package.json` 增加商用服务启动与测试脚本。

- 修改文件
  - `package.json`
  - `packages/service/package.json`
  - `packages/service/src/config.mjs`
  - `packages/service/src/http.mjs`
  - `packages/service/src/server.mjs`
  - `packages/service/src/store.mjs`
  - `packages/service/src/store.test.mjs`
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `node --test packages/service/src/*.test.mjs`：通过，2 个测试全部成功
  - `node -e "import('./packages/service/src/server.mjs')..."`：通过，模块可正常导入
  - HTTP 冒烟验证：通过，已确认 `GET /api/health`、`POST /api/tasks`、`GET /api/tasks/:id`、`POST /api/tasks/:id/messages`、`GET /api/tasks/:id/events`、`POST /api/tasks/:id/cancel` 正常返回

- 备注
  - 当前仍为本地文件存储骨架，尚未接入真实云上 OpenClaw Gateway、回调投递和持久化数据库。

## 15) 评估是否应先将 yuanyuan 部署到云上

- 对话目标
  - 回答用户关于“本地不便测试时，是否应先将 yuanyuan 上云”的判断问题，并结合仓库现有部署基线给出建议。

- 执行内容
  - 检查当前仓库的云部署相关文件，确认已存在：
    - 生产型容器编排
    - 环境变量与密钥校验脚本
    - 容器启动/停止/配置查看脚本
  - 核对 `prod` 方案中当前已覆盖的服务边界：
    - `savc-gateway`
    - `savc-ui`
    - 可选 `savc-proactive`
  - 明确当前新增的商用适配服务尚未接入 compose，因此更合理的顺序是先把 yuanyuan 云上基线跑稳，再把商用服务接进去。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `nl -ba infra/docker/README.md | sed -n '1,120p'`：通过，确认仓库已有云部署 Quick Start 与 Production 说明
  - `nl -ba infra/docker/docker-compose.prod.yml | sed -n '1,220p'`：通过，确认生产编排已覆盖 `savc-gateway`、`savc-ui`、`savc-proactive`
  - `nl -ba scripts/infra/prod_container.sh | sed -n '1,240p'`：通过，确认已有 `init-env`、`validate`、`up`、`down`、`restart`、`logs`、`ps`、`config`
  - `nl -ba scripts/infra/validate_cloud_env.sh | sed -n '1,240p'`：通过，确认网关 token、至少一个 LLM provider 与生产代理约束已具备

- 备注
  - 结论是“应先上云，但先部署 yuanyuan 核心基线，不要一开始把商用适配和真实业务后端全部一起捆上去”。

## 16) 评估 yuanyuan 当前的 CLI / Codex harness 调用能力

- 对话目标
  - 回答用户关于“yuanyuan 当前 CLI 调用能力如何，以及上云后是否适合直接通过对话驱动 Codex CLI 做自我开发”的问题。

- 执行内容
  - 检查 `yuanyuan` 当前的自驱开发脚本与权限前提，确认仓库已经具备：
    - `workspaceAccess=rw`
    - `tools.profile=coding`
    - `group:fs` / `group:runtime` 放通
    - 通过 `scripts/yuanyuan_autodev.sh` 执行“任务 -> 改代码 -> 验证 -> 修复回合”的基础链路
  - 检查 OpenClaw vendored 源码中的 ACP/acpx 能力，确认上游已经原生支持通过 `agentId: "codex"` 驱动外部 Codex harness。
  - 检查当前本机实际 OpenClaw 配置，确认：
    - 当前默认模型仍是 `volces/doubao-seed-1-8-251228`
    - 当前并未启用 `acp`
    - 当前也未启用 `plugins.entries.acpx`
  - 检查当前本机 `codex` 命令可用性，发现本地安装存在缺失依赖，当前环境下 `codex --help` 即失败，说明云上必须单独做 Codex 运行时安装与校验，不能假设“命令已可用”。
  - 检查 OpenClaw OAuth 文档，确认 OpenClaw 自身的 `openai-codex` OAuth 与 Codex CLI 登录同时使用时存在 refresh token 互相顶掉的风险。

- 修改文件
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `nl -ba scripts/yuanyuan_enable_autodev.sh | sed -n '1,320p'`：通过，确认 autodev 会写入 `workspaceAccess=rw`、`tools.profile=coding`、`group:fs/group:runtime`
  - `nl -ba scripts/yuanyuan_autodev.sh | sed -n '1,360p'`：通过，确认已有多轮开发/验证脚本
  - `nl -ba scripts/dev_preflight.sh | sed -n '100,240p'`：通过，确认已有 autodev readiness 检查
  - `nl -ba openclaw/extensions/acpx/skills/acp-router/SKILL.md | sed -n '1,260p'`：通过，确认 OpenClaw 已支持 `codex` ACP harness 路由
  - `nl -ba openclaw/docs/tools/acp-agents.md | sed -n '430,640p'`：通过，确认 ACP/acpx 权限、安装与默认 `codex` agent 说明
  - `python3 ... ~/.openclaw/openclaw.json`：通过，确认当前实际配置未启用 `acp` / `acpx`
  - `codex --help`：失败，当前本机 `codex` 缺少 `@openai/codex-linux-x64` 可选依赖
  - `nl -ba openclaw/docs/concepts/oauth.md | sed -n '1,140p'`：通过，确认 OpenClaw OAuth 与 Codex CLI 并行登录存在 token sink 风险提示

- 备注
  - 结论不是“直接 shell 硬调 codex”，而是“优先把 yuanyuan 云上化后，接通 ACP/acpx 的 `codex` harness 路径”，这样比 PTY/裸 CLI 控制更稳，也更适合后续扩展到其它 CLI。

## 17) 为云上 yuanyuan 接通 Codex ACP harness 基线

- 对话目标
  - 根据用户确认，开始把 yuanyuan 云上对接到 Codex CLI 的最小可运行链路，目标是后续可直接通过与 yuanyuan 对话触发 Codex 开发回路。

- 执行内容
  - 新增 `scripts/yuanyuan_enable_codex_acp.sh`，负责为现有 OpenClaw 配置补：
    - `acp.enabled`
    - `acp.backend=acpx`
    - `defaultAgent=codex`
    - `allowedAgents=["codex"]`
    - `plugins.entries.acpx.enabled=true`
    - `acpx` 的 `cwd`、`permissionMode`、`nonInteractivePermissions`
    - `~/.acpx/config.json` 中的 `codex -> codex-acp` 命令映射
  - 更新 `scripts/setup.sh`，增加 `SAVC_CODEX_ACP_*` 环境变量支持，并在启用开关时自动调用上述脚本。
  - 更新生产/云端 compose：
    - 为 gateway 增加 `SAVC_CODEX_ACP_*` 环境变量
    - 增加可选的 `/home/node/.codex` 持久化挂载
    - 为生产编排增加可选的持久化开发工作区 `/workspace-devrepo`
  - 更新 Docker gateway 镜像，在构建阶段预装：
    - `@openai/codex@0.114.0`
    - `@zed-industries/codex-acp@0.10.0`
  - 更新云环境模板与校验脚本，明确：
    - 云上启用 Codex ACP 时应提供 `OPENAI_API_KEY`
    - 生产自开发场景应提供持久化开发工作区
  - 更新容器说明文档，补充 Codex ACP 的云上启用方式。

- 修改文件
  - `scripts/yuanyuan_enable_codex_acp.sh`
  - `scripts/setup.sh`
  - `infra/docker/Dockerfile.gateway`
  - `infra/docker/docker-compose.prod.yml`
  - `infra/docker/docker-compose.cloud.yml`
  - `infra/docker/.env.prod.example`
  - `infra/docker/.env.example`
  - `infra/docker/README.md`
  - `infra/docker/bootstrap/empty-codex-home/.gitkeep`
  - `infra/docker/bootstrap/empty-workspace/.gitkeep`
  - `scripts/infra/validate_cloud_env.sh`
  - `config/.env.example`
  - `docs/worklog/2026-03-13.md`

- 验证结果
  - `bash -n scripts/yuanyuan_enable_codex_acp.sh scripts/setup.sh scripts/infra/validate_cloud_env.sh`：通过
  - `bash scripts/yuanyuan_enable_codex_acp.sh --dry-run ...`：通过，确认 dry-run 输出含 `acp/acpx` 配置
  - `env HOME=<tmp> SAVC_CODEX_ACP_ENABLE=1 bash scripts/setup.sh`：通过，确认生成的 `~/.openclaw/openclaw.json` 含 `acp.backend=acpx`，`~/.acpx/config.json` 含 `codex-acp` 命令映射
  - `docker compose -f infra/docker/docker-compose.prod.yml config`：通过
  - `docker compose -f infra/docker/docker-compose.cloud.yml config`：通过
  - `bash scripts/infra/validate_cloud_env.sh <temp-env>`：通过，确认启用 Codex ACP 时会检查 `OPENAI_API_KEY` 与持久化开发工作区

- 备注
  - 当前只是打通“云上 yuanyuan -> ACP/acpx -> Codex harness”的底座，还未执行真实 Codex 会话联调或云上镜像构建。
  - 云上 remote project 场景更适合 `OPENAI_API_KEY` 鉴权，不建议依赖 ChatGPT 订阅登录来驱动 Codex CLI。
