# 2026-03-08

- 对话目标：让 Git 忽略 `docs/openclaw-customPrmopt/` 下的大量未跟踪文件，减少待提交项干扰。
- 执行内容：检查仓库状态与忽略规则；确认 `docs/openclaw-customPrmopt/` 下约 4143 个未跟踪文件；将该目录加入本地 `.git/info/exclude`；补充工作日志索引与当日日志。
- 修改文件：`.git/info/exclude`、`docs/worklog/INDEX.md`、`docs/worklog/2026-03-08.md`
- 验证结果：已通过 `git status --short docs` 与 `git check-ignore -v docs/openclaw-customPrmopt/SAFE_SHORTLIST.md` 验证，该目录已被本地忽略，`docs/worklog` 仍正常显示跟踪变更。
- 备注：采用本地排除规则，避免影响仓库公共 `.gitignore`。

## 记录 2

- 对话目标：确认当前仓库变更的提交类型，并将当前待提交改动统一提交到 Git。
- 执行内容：检查 `git status`、`git diff --stat` 与核心 diff，确认当前改动以 OpenClaw/容器运行链路增强为主，提交类型归类为 `feat`；执行脚本语法检查与相关 Vitest；补充当日日志后，统一暂存当前工作区改动并创建提交。
- 修改文件：`docs/worklog/2026-03-08.md`
- 验证结果：执行 `bash -n scripts/setup.sh scripts/dev.sh scripts/openclaw.sh scripts/dev_preflight.sh scripts/llm_enable_failover.sh scripts/dev_container.sh` 通过；执行 `pnpm --dir openclaw exec vitest run src/agents/model-compat.test.ts src/agents/pi-embedded-helpers.formatassistanterrortext.test.ts` 通过；执行 `pnpm --dir openclaw exec vitest run --config vitest.e2e.config.ts src/auto-reply/reply.triggers.trigger-handling.runs-greeting-prompt-bare-reset.e2e.test.ts` 通过；尝试执行 `docker compose ... config` 时当前 WSL 环境提示未集成 Docker。
- 备注：提交类型采用 `feat`，因为本批改动包含 provider/compose/runtime 行为增强，不属于纯文档或单点修复。

## 记录 3

- 对话目标：为项目上云时复杂的 API 与凭据配置提供更稳妥的落地方案。
- 执行内容：检查当前 `infra/docker/.env.example`、`infra/docker/docker-compose.cloud.yml`、`scripts/setup.sh`、`config/channels.yaml`、`config/proactive.yaml` 与 `savc-ui` 中的环境变量读取方式；梳理现有配置入口、运行时注入路径与前端暴露面；基于仓库当前结构提出分层配置、Secrets Manager、`*_FILE` 文件型凭据、按 profile 启用模块以及前端 token 去敏的改造建议。
- 修改文件：`docs/worklog/2026-03-08.md`
- 验证结果：已确认网关、主动引擎与前端分别通过环境变量读取 provider key、bot token 与日历/天气凭据；已定位 `savc-ui` 直接读取 `VITE_SAVC_GATEWAY_TOKEN`，该值在云端会暴露给浏览器；已确认 Google Calendar 当前使用的是服务账号文件路径而非 JSON 内容。
- 备注：本轮未改业务代码，输出为部署与配置治理建议。

## 记录 4

- 对话目标：直接落地项目上云的完整版本，包括文件型 secret、前端去敏代理、生产 compose、部署校验与文档说明，并在完工后统一执行回归测试。
- 执行内容：新增 `scripts/lib/secret_env.sh` 为 `setup/dev/openclaw/llm_enable_failover` 提供 `*_FILE` secret 解析；为 `savc-ui` 新增网关 URL 解析模块并移除浏览器侧 `VITE_SAVC_GATEWAY_TOKEN` 依赖；在 `savc-ui/vite.config.ts` 中实现 `/gateway` 的 HTTP/WS 服务端代理，由 Vite 服务器持有 shared token 并在首个 `connect` 帧中注入；为主动引擎补充 `OPENWEATHER_API_KEY_FILE` 与 `GOOGLE_CALENDAR_SERVICE_ACCOUNT_JSON_FILE` 支持，并兼容旧的服务账号路径/JSON 传法；新增 `.dockerignore`、生产 `.env` 示例、`docker-compose.prod.yml`、`Dockerfile.gateway`、`Dockerfile.runtime`、`prod_container.sh`、`validate_cloud_env.sh`；同步更新现有 cloud compose、README、`.gitignore` 与依赖锁文件。
- 修改文件：`.gitignore`、`.dockerignore`、`config/proactive.yaml`、`infra/docker/.env.example`、`infra/docker/.env.prod.example`、`infra/docker/README.md`、`infra/docker/docker-compose.cloud.yml`、`infra/docker/docker-compose.prod.yml`、`infra/docker/Dockerfile.gateway`、`infra/docker/Dockerfile.runtime`、`pnpm-lock.yaml`、`savc-ui/package.json`、`savc-ui/src/ui/gateway-url.ts`、`savc-ui/src/ui/app.ts`、`savc-ui/src/ui/live2d-bridge.ts`、`savc-ui/src/ui/mock/index.ts`、`savc-ui/src/ui/views/logs.ts`、`savc-ui/vite.config.ts`、`scripts/lib/secret_env.sh`、`scripts/setup.sh`、`scripts/dev.sh`、`scripts/openclaw.sh`、`scripts/llm_enable_failover.sh`、`scripts/dev_preflight.sh`、`scripts/proactive_daemon.mjs`、`scripts/prod_container.sh`、`scripts/validate_cloud_env.sh`、`docs/worklog/2026-03-08.md`
- 验证结果：执行 `pnpm install --lockfile-only` 与 `pnpm install --filter savc-management-ui` 完成依赖同步；执行 `bash -n scripts/setup.sh scripts/dev.sh scripts/openclaw.sh scripts/dev_preflight.sh scripts/llm_enable_failover.sh scripts/prod_container.sh scripts/validate_cloud_env.sh scripts/lib/secret_env.sh` 通过；执行 `node --check scripts/proactive_daemon.mjs` 通过；基于临时 secret 目录执行 `bash scripts/validate_cloud_env.sh <temp-env>` 通过（PASS 5 / WARN 3 / FAIL 0）；执行 `pnpm --dir savc-ui build` 通过；执行 `pnpm --dir openclaw exec vitest run src/agents/model-compat.test.ts src/agents/pi-embedded-helpers.formatassistanterrortext.test.ts` 与 `pnpm --dir openclaw exec vitest run --config vitest.e2e.config.ts src/auto-reply/reply.triggers.trigger-handling.runs-greeting-prompt-bare-reset.e2e.test.ts` 均通过。
- 备注：当前本机 WSL 环境仍无可用 Docker 集成，因此本轮未实际执行 `docker compose` 启动或 `docker compose ... config` 的最终验证；相关入口脚本和 env 校验链已补齐。

## 记录 5

- 对话目标：说明 `infra/docker/.env.prod` 实际需要填写哪些配置，区分最小必填项与按需可选项。
- 执行内容：读取 `.env.prod.example`、`docker-compose.prod.yml` 与 `validate_cloud_env.sh`，梳理生产模板中网关/UI/自动化三类配置的消费位置；整理出“最小可运行配置”“可选 secret 文件”“保持默认即可”的说明口径。
- 修改文件：`docs/worklog/2026-03-08.md`
- 验证结果：已核对 `OPENCLAW_GATEWAY_TOKEN_FILE`、至少一个 LLM provider secret、`VITE_SAVC_GATEWAY_URL=/gateway` 是当前 prod 校验脚本的核心约束；已确认 `GOOGLE_CALENDAR_ID`、`OPENWEATHER_*`、Discord/Telegram token 都属于按需配置。
- 备注：本轮未改业务代码，仅补充使用说明与日志。
