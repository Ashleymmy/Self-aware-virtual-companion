# 2026-03-12

## 1) 盘点当前项目状态与新版 OpenClaw 接入情况

- 对话目标
  - 帮用户快速确认当前仓库整体状态，并对照 `docs/separation/` 中的迁移文档检查新版 OpenClaw 落位后的实际可用性。

- 执行内容
  - 检查仓库顶层结构、`git status`、`README.md`、`package.json`、`pnpm-workspace.yaml`，确认项目正在从旧的 `savc-core/savc-ui/openclaw/extensions` 布局迁移到 `packages/core`、`packages/ui`、`packages/plugin`。
  - 阅读 `docs/separation/openclaw-interface.md`、`docs/separation/inventory.md`、`docs/separation/migration-guide.md`、`docs/separation/architecture.md`，核对文档中的拆分目标与当前目录现实。
  - 检查 `packages/plugin/src/paths.ts` 与 `packages/plugin/src/real-session-adapter.ts`，确认插件已适配新布局，并支持通过 `OPENCLAW_ROOT` 或 `openclaw` 包解析新版 OpenClaw 内部模块。
  - 检查新版 OpenClaw 实际落位目录，确认当前源码根在 `openclaw/openclaw-main/openclaw-main/`，而不是仓库脚本默认使用的 `openclaw/`。
  - 验证新版 OpenClaw 中 `OpenClawPluginApi`、`createSessionsSpawnTool`、`createSessionsSendTool`、`callGateway` 仍然存在，判断当前主要阻塞点是路径而不是接口消失。

- 修改文件
  - `docs/worklog/2026-03-12.md`
  - `docs/worklog/INDEX.md`

- 验证结果
  - `bash scripts/dev_preflight.sh`：`PASS: 16 / WARN: 0 / FAIL: 0`
  - 路径校验：`openclaw/package.json` 不存在，`openclaw/openclaw-main/openclaw-main/package.json` 存在
  - 静态核对：`scripts/dev.sh` 与 `scripts/setup.sh` 默认仍以 `OPENCLAW_ROOT` 或 `./openclaw` 作为 OpenClaw 根目录
  - 静态核对：新版 OpenClaw `2026.3.9` 中仍包含 `src/plugins/types.ts`、`src/agents/tools/sessions-spawn-tool.ts`、`src/agents/tools/sessions-send-tool.ts`、`src/gateway/call.ts`

- 备注
  - 当前仓库存在大规模未提交迁移变更；本轮只做状态盘点，没有改动业务代码。

## 2) 实施 separation 收口计划并完成主链路验收

- 对话目标
  - 按 separation 计划把仓库默认基线收口到 `openclaw/`、`packages/core`、`packages/ui`、`packages/plugin`、`scripts/{lifecycle,runtime,test}`，并完成 `setup/dev/test/docker` 主链路验收。

- 执行内容
  - 将新版 OpenClaw 拍平到仓库根下 `openclaw/`，统一所有脚本、测试、Docker 对默认 `OPENCLAW_ROOT=${REPO_ROOT}/openclaw` 的约定。
  - 新增 `scripts/lib/paths.sh`，统一 `REPO_ROOT` / `OPENCLAW_ROOT` 解析与 OpenClaw readiness（依赖安装、最小构建、错误提示）逻辑，并接入 `scripts/dev.sh`、`scripts/setup.sh`、`scripts/openclaw.sh`、`scripts/dev_preflight.sh`、`scripts/dev_self_upgrade.sh`、`scripts/savc_ui_service.sh`、`scripts/yuanyuan_autodev.sh` 及 `scripts/test/*`、`scripts/lifecycle/*`、`scripts/infra/*`。
  - 批量收口旧路径引用，切掉 `savc-core` / `savc-ui` / `scripts/proactive_*.mjs` / `scripts/memory_semantic.mjs` 等旧路径，统一到 `packages/*`、`scripts/runtime/*`、`scripts/lifecycle/*`、`scripts/test/*`。
  - 更新 `packages/plugin` 测试依赖与本地 OpenClaw 开发依赖，修复 `tool-voice-call` 的 OpenClaw 模块解析与 `operator.admin` scope 透传。
  - 修复 `scripts/runtime/tool_learner_runtime.mjs`，为内部 OpenClaw CLI 调用提供隔离 `HOME/STATE/CONFIG`，并改成基于临时文件捕获 stdout/stderr，消除 Phase 3/4a 的空输出与用户主目录污染问题。
  - 修复 `scripts/runtime/proactive_daemon.mjs` 对旧 `scripts/proactive_dispatcher.mjs` 的硬编码；将 `scripts/test/test_phase2.sh` 的 live smoke 默认模式改为缺少外部凭据时 `WARN` 而非默认失败。
  - 修复 OpenClaw vendor 中 `voice-call` 两个收口问题：一是 `continueCall()` 先挂 transcript waiter 再 speak/startListening，消除 webhook 早到竞态；二是 `MockProvider.verifyWebhook()` 生成稳定 `verifiedRequestKey`，使本地 mock webhook 能通过当前安全校验。
  - 更新 README、`docs/separation/*`、Dockerfile / compose，使文档、开发命令、运行入口与当前落地结构一致。

- 修改文件
  - `README.md`
  - `docs/separation/architecture.md`
  - `docs/separation/inventory.md`
  - `docs/separation/migration-guide.md`
  - `docs/separation/openclaw-interface.md`
  - `scripts/lib/paths.sh`
  - `scripts/dev.sh`
  - `scripts/setup.sh`
  - `scripts/openclaw.sh`
  - `scripts/dev_preflight.sh`
  - `scripts/dev_self_upgrade.sh`
  - `scripts/savc_ui_service.sh`
  - `scripts/yuanyuan_autodev.sh`
  - `scripts/runtime/proactive_daemon.mjs`
  - `scripts/runtime/tool_learner_runtime.mjs`
  - `scripts/test/test_phase2.sh`
  - `scripts/test/test_phase4b_plugin_real.sh`
  - `scripts/test/test_phase5d.sh`
  - `scripts/test/test_phase5e.sh`
  - `scripts/test/test_phase6.sh`
  - `packages/plugin/package.json`
  - `packages/plugin/src/tool-voice-call.ts`
  - `packages/plugin/src/tool-voice-call.test.ts`
  - `packages/ui/vite.config.ts`
  - `tests/orchestrator/lifecycle.test.mjs`
  - `tests/orchestrator/decomposer.test.mjs`
  - `tests/orchestrator/voice.test.mjs`
  - `tests/orchestrator/registry.test.mjs`
  - `tests/orchestrator/vibe-coder.test.mjs`
  - `tests/orchestrator/router.test.mjs`
  - `tests/orchestrator/vision.test.mjs`
  - `tests/orchestrator/live2d-voice-chain.test.mjs`
  - `tests/orchestrator/aggregator.test.mjs`
  - `tests/orchestrator/live2d.test.mjs`
  - `tests/skills/memory-semantic.test.mjs`
  - `tests/skills/memory-semantic-local.test.mjs`
  - `infra/docker/Dockerfile.gateway`
  - `infra/docker/Dockerfile.runtime`
  - `infra/docker/docker-compose.prod.yml`
  - `infra/docker/docker-compose.cloud.yml`
  - `infra/docker/openclaw.container.json`
  - `infra/docker/README.md`
  - `openclaw/extensions/voice-call/src/manager/outbound.ts`
  - `openclaw/extensions/voice-call/src/manager.closed-loop.test.ts`
  - `openclaw/extensions/voice-call/src/providers/mock.ts`
  - `openclaw/extensions/voice-call/src/providers/mock.test.ts`
  - `openclaw/src/gateway/net.ts`
  - `openclaw/src/infra/tailnet.ts`
  - `openclaw/`（vendor snapshot 拍平，涉及 `package.json`、`openclaw.mjs`、`src/`、`dist/` 等目录）
  - `pnpm-lock.yaml`
  - `docs/worklog/2026-03-12.md`

- 验证结果
  - `CI=true pnpm install --no-frozen-lockfile`：通过
  - `pnpm -C openclaw install --frozen-lockfile`：通过
  - `pnpm -C openclaw exec tsdown --no-clean`：通过（多次重建）
  - `pnpm dev:check`：通过（`PASS: 17 / WARN: 0 / FAIL: 0`）
  - `HOME=/tmp/... bash scripts/setup.sh`：通过
  - `timeout 20s bash scripts/dev.sh`：通过 readiness / build / startup 烟雾验证
  - `cd packages/plugin && pnpm exec vitest run src/*.test.ts`：通过（`8 files / 30 tests`）
  - `pnpm test:phase2`：通过（`PASS: 13 / WARN: 2 / FAIL: 0`）
  - `pnpm test:phase3`：通过（`PASS: 32 / FAIL: 0`）
  - `pnpm test:phase4a`：通过（`PASS: 25 / FAIL: 0`）
  - `pnpm test:phase4b:plugin`：通过
  - `pnpm test:phase4b:plugin:real`：通过（`PASS: 13 / WARN: 1 / FAIL: 0`）
  - `pnpm test:phase5`：通过
  - `pnpm test:phase6`：通过（`PASS: 31 / FAIL: 0`）
  - `pnpm test:status`：通过，快照为 `Phase 1/2/3 = PASS`
  - `docker compose -f infra/docker/docker-compose.prod.yml config`：通过
  - `docker compose -f infra/docker/docker-compose.cloud.yml config`：通过

- 备注
  - `phase2` 的真实天气/日历烟雾校验仍依赖外部环境变量；当前默认基线改为缺失凭据时 `WARN`，如需强制 live 校验可显式设置 `PHASE2_LIVE_STRICT=1`。

## 3) 按修改类型整理工作区并分类提交

- 对话目标
  - 将当前大规模迁移工作按文件类型和变更职责拆成可读的分类提交，便于后续审阅、回滚和继续推进。

- 执行内容
  - 盘点当前工作区改动范围，确认主要分为三类：`openclaw/` vendor 快照更新、SAVC 工作区与脚本分层迁移、文档与 Docker/工作日志对齐。
  - 按分组准备分类提交，避免把 vendor、代码迁移、文档/基础设施变更混成一个超大 commit。
  - 保持旧目录删除与新目录新增在同一类提交中，确保每个提交内部尽量自洽。

- 修改文件
  - `docs/worklog/2026-03-12.md`

- 验证结果
  - `git status --short`：已完成工作区分组盘点
  - `git diff --stat`：确认当前改动可按 vendor / workspace-code / docs-infra 三类拆分

- 备注
  - 本节记录的是提交整理动作本身；具体提交结果以本轮后续 git commit 为准。
