# Claude Code 工作日志 — 2026-03-10

## 记录 01：SAVC 二开代码剥离文档编写

### 对话目标
项目因二次开发导致自定义代码与 OpenClaw 框架代码混合，用户需要将二开代码剥离并接入新版 OpenClaw。本次任务是全面分析项目结构，产出剥离参考文档。

### 执行内容
1. 全面探索项目结构，识别自定义代码 vs OpenClaw 框架代码
2. 深入分析 savc-orchestrator 插件与 OpenClaw 的 7 个对接点
3. 逐文件读取关键集成代码（real-session-adapter.ts、paths.ts、gateway-ws.ts、setup.sh）
4. 提取 7 个 Tool 的完整参数 schema 和返回值类型
5. 编写 4 份剥离参考文档

### 修改文件
- `docs/separation/inventory.md` — 代码资产清单（自定义代码 / 框架代码 / 需修改文件）
- `docs/separation/openclaw-interface.md` — OpenClaw 对接接口文档（Plugin SDK / 7 个 Tool Schema / 内部 API / WebSocket 协议 / HTTP API / 配置 schema / CLI 接口）
- `docs/separation/migration-guide.md` — 新版 OpenClaw 迁移指南（6 步操作 + 9 项兼容性检查清单 + 端到端验证流程）
- `docs/separation/architecture.md` — 分离后架构概览（架构图 / 4 条数据流图 / 依赖强度分级 / 风险点总结）
- `docs/worklog/claude/2026-03-10.md` — 本日志

### 验证结果
- 4 个文档文件均已成功写入 `docs/separation/` 目录
- 内容覆盖了所有已知的对接点（Plugin SDK、内部 API、WebSocket、HTTP、CLI、配置生成、运行时目录）
- 标注了风险最高的 `real-session-adapter.ts` 中 3 处内部 API 导入

### 备注
- 当前 `openclaw/` 是内联拷贝（非 git submodule），推荐迁移为 git submodule
- 最高风险点是 `real-session-adapter.ts` 对 OpenClaw 内部模块的直接导入，建议后续请求上游将 `createSessionsSpawnTool`、`createSessionsSendTool`、`callGateway` 暴露到 `openclaw/plugin-sdk`
