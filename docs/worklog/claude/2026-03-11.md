# Claude Code 工作日志 — 2026-03-11

## 记录 01：项目重构路径引用批量更新（子任务）

### 对话目标
根据项目目录重构计划，批量更新 scripts/ 下测试、生命周期和运行时脚本中的路径引用。

### 执行内容
按照以下 5 条重构映射规则，更新了 scripts/ 下 25 个文件中的路径引用：
1. `savc-core/` -> `packages/core/`
2. `savc-ui/` -> `packages/ui/`
3. `openclaw/extensions/savc-orchestrator/` -> `packages/plugin/`
4. `config/openclaw/agents/` -> `config/agents/`（未发现匹配项）
5. `scripts/memory_semantic.mjs` -> `scripts/runtime/memory_semantic.mjs`

（详细清单见修改文件部分，已由子 Agent 执行）

---

## 记录 02：SAVC 彻底剥离 + 项目规范化重构（主任务）

### 对话目标
将 SAVC 二开代码从 OpenClaw 框架中彻底剥离，重组为规范的 packages/ monorepo 结构，便于后续独立开发。

### 执行内容

**Phase 1 — 移动插件**
- `openclaw/extensions/savc-orchestrator/` → `packages/plugin/`
- package.json: `@openclaw/savc-orchestrator` → `@savc/plugin`，移除 `workspace:*` 依赖
- `real-session-adapter.ts`：3 处内部 API 导入改为 `resolveOpenClawModule()` 统一解析
- `paths.ts`：`findRepoRoot()` 种子路径适配新布局、兼容新旧 memory_semantic.mjs 路径

**Phase 2 — 删除 OpenClaw**
- 删除 `openclaw/` 整个目录 + `openclaw-history.bundle`
- 创建 `openclaw/README.md` 占位（说明 3 种接入方式）

**Phase 3 — 重组 monorepo**
- `savc-core/` → `packages/core/`、`savc-ui/` → `packages/ui/`
- `pnpm-workspace.yaml` → `packages/*`

**Phase 4 — 整理 config**
- `config/openclaw/agents/` → `config/agents/`（9 个 SOUL.md 模板）

**Phase 5 — 整理 scripts**
- 创建 4 个子目录：`runtime/`（8 个）、`test/`（17 个）、`lifecycle/`（9 个）、`infra/`（6 个）

**Phase 6-7 — 路径引用全量更新**
- `setup.sh`、`dev.sh`、`dev_preflight.sh` 等核心脚本
- 所有 runtime/lifecycle/test 脚本（25+ 个文件）
- `packages/core/orchestrator/*.mjs` 默认 agentsDir
- `package.json` 50 条 npm scripts
- Docker、.gitignore、proactive.yaml

**Phase 8 — 文档更新**
- `CLAUDE.md` 添加新项目结构
- 本工作日志

### 修改文件

**物理移动**（4 大块 + 26 个脚本进入子目录）：
- `openclaw/extensions/savc-orchestrator/` → `packages/plugin/`
- `savc-core/` → `packages/core/`
- `savc-ui/` → `packages/ui/`
- `config/openclaw/agents/` → `config/agents/`

**删除**：`openclaw/`（框架源码）、`openclaw-history.bundle`

**编辑**（核心文件 30+）：
- packages/plugin/: `package.json`, `src/real-session-adapter.ts`, `src/paths.ts`, `openclaw.plugin.json`
- packages/core/: `package.json`, `orchestrator/{registry,router,decomposer}.mjs`
- packages/ui/: `package.json`
- 根目录: `package.json`, `pnpm-workspace.yaml`, `.gitignore`, `CLAUDE.md`
- scripts/: `setup.sh`, `dev.sh`, `dev_preflight.sh`, `dev_self_upgrade.sh`, `savc_ui_service.sh`, `build_phase1_prompt.sh`
- scripts/runtime/: 全部 8 个 .mjs 文件
- scripts/test/: 13+ 个 .sh 文件
- scripts/lifecycle/: `phase2_run_once.sh`, `phase3_run_daily.sh`, `phase3_run_monthly.sh`, `phase4b_enable_plugin.sh`, `phase5_enable_plugins.sh`
- scripts/infra/: `bootstrap-mac.sh`
- infra/docker/: `Dockerfile.gateway`
- config/: `proactive.yaml`
- 新增: `openclaw/README.md`

### 验证结果
- `grep -rn 'savc-core' scripts/ packages/ config/` → **0 条结果**
- `ls packages/{core,ui,plugin}` → 三个包就位
- `ls scripts/{runtime,test,lifecycle,infra}` → 四个子目录均有文件
- `cat pnpm-workspace.yaml` → `packages/*`
- `ls openclaw/` → 仅 README.md

### 备注
- `docs/separation/` 下的 4 份文档记录的是重构前布局，可作为历史参考
- `real-session-adapter.ts` 改为多策略解析，接入新版 OpenClaw 时设置 `OPENCLAW_ROOT` 环境变量即可
- Docker 配置已更新但未测试构建（用户确认容器运行中不受影响）
