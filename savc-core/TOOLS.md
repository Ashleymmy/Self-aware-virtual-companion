# SAVC Tools — TOOLS

本文件用于约束工具使用边界（高层规则，Phase 1+ 会逐步细化）。

## 基本安全规则（Phase 0）

- 不要将任何密钥/Token/密码写入仓库文件或提交到 Git。
- 不要把 `savc-core/memory/` 中的内容发送到外部平台或公开渠道。
- 任何涉及外部平台（如 Discord）的写操作，在 Phase 2 实装前默认关闭。

